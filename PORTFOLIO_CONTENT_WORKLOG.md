# 포트폴리오 콘텐츠 인터뷰 작업 노트

마지막 업데이트: 2026-09-01

## 목적

현재 포트폴리오의 과장되거나 AI가 쓴 듯한 문장을 실제 경험과 판단이 드러나는 내용으로 다시 작성한다.

이 문서는 공개용 카피가 아니라 다음 작업을 이어가기 위한 내부 메모다. 확인되지 않은 내용은 포트폴리오에 바로 반영하지 않는다.

## 작업 원칙

- 한 번에 프로젝트 하나, 질문 하나씩 진행한다.
- 먼저 사실과 당시 상황을 기록하고, 문장은 마지막에 다듬는다.
- 사용자가 직접 작성하지 않았거나 설명하기 어려운 코드는 대표 코드로 사용하지 않는다.
- 대표 코드는 나중에 사용자가 직접 작성한 옛 코드 중에서 사용자가 후보를 고른다.
- 회사 저장소의 코드는 공개 가능 여부를 별도로 확인한다.
- AI가 제안하거나 작성한 설계를 사용자가 직접 만든 것처럼 표현하지 않는다.
- 사용자가 문제를 정의하고 결정·검증·운영한 부분과, 직접 구현한 부분을 구분한다.

## 현재 사이트에서 바로잡아야 할 부분

### 비용 수치

`app/page.tsx`의 「철강 스크랩 AI 검수 시스템 재구축」에는 현재 다음 수치가 적혀 있다.

- 월 비용 약 6,000만 원 → 500만 원
- 서버 56대 → 10여 대

첨부된 실제 청구 화면에서 확인한 값은 다음과 같다.

- 2024년 11월 사용 요금 합계: 55,241,140원
- 2026년 6월 사용 요금 합계: 10,524,650원
- 월 비용 차이: 44,716,490원
- 감소율: 약 80.9%

두 비용은 기준이 다르므로 분리해서 다룬다.

- 실제 청구액 기준: `5,524만 원 → 1,052만 원`, 약 80.9% 감소
- 계약 보전용 임시 서버를 제외한 필수 워크로드 기준: 월 500만 원 미만이라는 사용자 진술

기존 클라우드 계약의 월 약정 금액은 1,300만 원이었다. 실제 필요한 인프라 비용이 약정 금액보다 낮아지자 사용하지 않는 계약 금액을 맞추기 위해 GPU 학습 서버 1대를 임시로 실행했다. 따라서 2026년 6월의 1,052만 원은 기술적으로 필요한 최저 운영 비용이 아니며, 최적화 한계로 표현하면 안 된다.

청구 화면은 실제 지출을 증명하고, 월 500만 원 미만은 계약 보전용 자원을 제외한 기술적 run-rate를 설명한다. 공개할 때는 두 기준을 한 문장에 섞지 않는다.

공개 범위 확인:

- 실제 청구액 `5,524만 원 → 1,052만 원`: 공개 가능
- 계약 보전용 임시 자원을 제외한 필수 워크로드 `월 500만 원 미만`: 공개 가능
- 클라우드 월 약정금액 `약 1,300만 원`: 공개하지 않음
- 공개 문장에서는 구체적인 약정금액 없이 `계약 조건으로 유지한 임시 자원 제외` 정도로만 설명

청구 화면 원본은 임시 파일로 전달되었으며 경로가 사라질 수 있으므로 위 숫자를 이 문서에 보존한다.

### 코드 갤러리

- 현재 대표 코드 13개는 사용자가 직접 선정한 것이 아니다.
- AI가 만든 코드 또는 사용자가 모르는 코드가 포함되어 있으므로 그대로 실력 증거로 사용하면 안 된다.
- 코드 작업은 보류한다.
- 추후 사용자가 직접 작성했고 공개 가능한 옛 코드만 후보로 받는다.
- `Code, as art.`와 `실제 저장소에서 선별한 코드`라는 현재 메시지도 대표 코드 선정 후 다시 판단한다.

## 프로젝트 인터뷰 01: 철강 스크랩 AI 검수 시스템

### 팀과 배경

- 사용자가 3명 팀을 이끌어 POC를 성공시켰다.
- POC 성공 후 약 50명 규모의 다른 팀과 합류해 본사업을 진행했다.
- 검수 영역은 사용자의 3명 팀이 계속 담당했고, 그 밖의 영역은 합류한 대규모 팀이 담당했다.
- 이후 약 50명 규모의 팀이 프로젝트에서 철수했다.
- 철수 당시 시스템에는 지나치게 넓은 범위의 잘못된 설계와 과도한 운영 비용이 남아 있었다.
- 사용자는 남은 구조를 인수해 처리 속도를 개선하면서 비용을 줄이는 최적화 작업을 진행했다.
- 2024년 1월 POC를 시작해 2024년 3월 성공시켰다.
- 2024년 4월부터 5월 초 사이 약 50명 규모의 팀이 구성됐고, 사용자 팀의 소스가 기본 베이스로 전달됐다.
- 약 50명 규모의 팀은 2025년 6월 철수했다.
- 사용자는 2025년 7월 인수인계를 받고 최적화를 시작했다.

3명 팀의 역할:

- 사용자: 프로젝트 리드. 핵심 시스템 설계, 개발, 서비스 배포와 현장 대응까지 전체 책임
- 인프라 담당 1명: Kubernetes 등 인프라 영역의 구체적인 구축과 운영 담당
- 보조 개발자 1명: `aimos-fofe`의 미터링·빌링, 대시보드, 사용자 추가 등 운영·관리 기능 담당

사용자는 `AIM`, `aimos-aia`, `aimos-aib`, `aimos-fofe`, `aimos-fomo`, `aimos-ais`, `aimos-aid` 전반의 설계와 핵심 구현을 직접 담당했다. 제품과 자동 검수 흐름의 설계·개발·배포·현장 대응을 모두 주도했다.

다만 모든 코드를 혼자 작성했다는 뜻으로 과장하지 않는다.

- 보조 개발자는 `aimos-fofe`의 미터링·빌링, 대시보드, 사용자 추가 등 일부 운영·관리 기능에 기여
- 인프라 담당자는 Kubernetes 클러스터와 내부 DB·Redis·RabbitMQ의 구체적인 구축을 담당

공개 문장에서는 사용자의 전체 시스템 오너십과 직접 구현 범위를 강조하되, 위 협업 기여를 지우지 않는다.

### 확대된 구조에서 발견한 문제

- 총 56대까지 늘어난 서버와 지나치게 분리된 배포 단위
- 시스템 호출이 여러 구성요소를 돌고 도는 구조
- 모델 응답이 MQ를 거쳐 Edge별 전용 큐와 처리 Docker로 다시 전달되는 구조
- Edge마다 큐와 처리 Docker가 하나씩 필요했고, 각 Docker는 작업을 동기로 하나씩 처리
- Edge별 처리 Docker의 상태를 감지하기 위한 별도 서버까지 운영
- 3초마다 MQ로 health 메시지를 보내고 DB에 행을 계속 추가하는 모니터링
- 운영 DB에 로그까지 계속 적재
- 목적에 비해 불필요했던 Airflow와 Superset
- 파일 요청이 애플리케이션 서버를 불필요하게 경유
- 리버스 프록시를 거치는 복잡한 호출 흐름
- 장기 보관 데이터에도 일반 Object Storage를 사용
- 분리된 DB로 인한 관리와 조회 복잡도
- Admin 기능 자체는 필요했으므로 무조건 불필요한 구성으로 분류하지 않는다.

### 재설계 원칙과 실행 내용

구성요소를 기술 스택이 아니라 실제 책임과 부하 특성으로 다시 구분했다.

이 작업의 목적은 단순한 비용 절감이 아니었다. 과도하게 나뉜 호출과 배포 구조를 줄여 처리 속도를 개선하고, 같은 과정에서 불필요한 인프라 비용도 함께 제거하는 것이었다.

- 제품 Backend API
- AI 추론 서버
- AI 후처리 서버
- NCP Cloud Functions로 분리한 ZIP 생성, 썸네일 생성 등 CPU 집약 작업
- 정적 Frontend
- 파일 전송
- 로그와 모니터링
- 장기 데이터 보관

실행 내용:

- 첫 최적화 대상으로 Edge별 모델 응답 처리 큐와 Docker를 중앙 서비스로 통합
- 모델 요청과 모델 결과 처리를 하나의 중앙 서비스에서 담당하도록 변경
- 현재 이 중앙 처리 역할은 `aimos-aib`가 담당
- 큐에는 이미지 바이너리 대신 다운로드용 Presigned URL을 전달
- 모델 서버가 처리 결과 이미지도 업로드용 Presigned URL로 저장하도록 구성
- Reverse Proxy 중심 흐름을 Backend API 호출 중심으로 단순화
- Frontend를 CloudFront로 이전
- Presigned URL을 사용해 파일 트래픽을 애플리케이션 서버에서 분리
- 로그를 운영 DB에서 분리하고 Grafana 기반 관찰 경로로 이전
- Edge별 Docker를 감시하던 별도 서버를 제거하고 Grafana 중심 모니터링으로 전환하도록 지시
- Slack 알림 도입
- DB 병합과 정규화
- Archive Storage 적극 활용
- 최적화 이전 집계는 학습용 GPU 서버 4대를 포함한 총 56대였으며, 현장 컴퓨터는 이 숫자에서 제외
- 사용자 팀이 Detection과 OCR 등 AI 실행 영역을 다시 가져오면서 GPU 서버를 4대에서 2대로 축소
- SageMaker 전환 후 상시 운영 GPU 서버를 0대로 변경
- 기존 클라우드 계약의 월 약정 금액이 약 1,300만 원이어서, 사용하지 않는 계약 금액을 맞추기 위한 GPU 학습 서버 1대를 임시로 추가한 예외가 있었음
- SageMaker 기반 GPU 추론 비용은 월 300만 원대로 변경
- 최종 클라우드 콘솔 화면에서 운영 중인 KVM 인스턴스는 총 10대
- 화면의 서버 이름 기준으로 일반 Kubernetes 워커 6대, CI 워커 1대, DB 워커 3대로 구성된 것으로 보임
- Kubernetes 전환과 구체적인 클러스터 구축은 인프라 담당자가 구현
- 사용자는 Kubernetes 구축 자체가 아니라, 통합할 수 있도록 서비스 책임과 처리 경계를 정리하는 기초 작업을 담당
- 정적 Frontend를 CloudFront로 이전
- 기존에 과도하게 분할돼 있던 서버를 역할 기준으로 통합
- 불필요한 Load Balancer와 서비스 타입 VPN 계층 제거
- 관리형 서비스 비용을 줄이기 위해 DB, Redis, RabbitMQ를 Kubernetes 내부에서 운영하는 구조로 전환하는 방향 결정
- 이 토폴로지 단순화 이후 인프라 담당자가 Kubernetes 통합을 구현
- Kubernetes 내부 DB, Redis, RabbitMQ의 구체적인 구축과 운영 구성은 인프라 담당자가 구현

확인 필요:

- Kubernetes 전환 이전 사용자가 만든 기초 작업의 정확한 범위

### 첫 최적화: Edge별 동기 큐 처리 통합

기존 흐름:

`모델 요청 → 모델 응답 → MQ → Edge별 처리 큐 → Edge별 Docker가 동기로 하나씩 처리`

- 처리 단위는 독립 서버라기보다 큐 소비를 위해 실행한 Docker에 가까웠음
- Edge 하나마다 큐와 처리 Docker가 하나씩 필요했음
- 이 Docker의 상태를 감지하기 위한 별도 서버도 운영했음
- 처리 단위와 감시 단위가 Edge 수에 따라 함께 증가하는 구조였음

변경 흐름:

`중앙 서비스에서 모델 요청 → 큐로 다운로드 Presigned URL 전달 → 모델 처리 → 업로드 Presigned URL로 결과 저장 → 중앙 서비스에서 결과 처리`

- Edge별 처리 큐와 Docker를 중앙 서비스로 통합
- 모델 요청과 결과 처리를 한곳에서 담당
- 이미지 전송은 Object Storage의 Presigned URL로 분리
- 현재 중앙 처리 역할은 `aimos-aib`가 담당
- 별도 감시 서버 대신 Grafana 모니터링과 Slack 알림 사용

이 변경은 Edge 수만큼 처리·감시 단위가 늘어나는 구조를 제거한 첫 번째 최적화 작업이었다.

### NCP Cloud Functions로 분리한 CPU 작업

AWS에서는 이미지 요청 시 GET 파라미터로 크기를 전달하면 Lambda를 통해 동적으로 리사이즈하는 구성을 사용할 수 있었다. NCP에는 당시 같은 방식이 없어 별도의 흐름을 만들었다.

- 큰 원본 이미지가 업로드되면 NCP Cloud Functions가 리사이즈된 썸네일 생성
- 생성된 썸네일은 CDN을 통해 제공
- 검출 이미지를 모델 학습용으로 수집할 때 필요한 ZIP 파일도 애플리케이션 서버에서 만들지 않음
- ZIP 생성 요청을 NCP Cloud Functions로 보내 CPU 집약 작업을 별도 실행 환경으로 분리

핵심 판단은 이미지 변환과 대용량 압축 작업이 제품 Backend API의 CPU와 응답 시간을 점유하지 않도록 실행 경계를 나눈 것이다.

분리 전후의 CPU 사용률, 응답 시간 또는 장애 빈도를 측정한 자료는 없다. 공개 문장에서는 효과가 검증됐다고 쓰지 않고, CPU 집약 작업을 제품 Backend API에서 분리한 구조적 최적화로만 설명한다.

확인된 사용자의 기초 작업 범위:

- Frontend의 CloudFront 이전
- 분할 서버 통합
- Load Balancer 제거
- 서비스 타입 VPN 제거

`서비스 타입 VPN`의 정확한 제품명 또는 구성 명칭은 추가 확인이 필요하다.

기존 요청 경로와 제품명은 사용자가 정확히 기억하지 못한다. 공개 문장에서는 구체적인 호출 순서나 제품명을 추정하지 않고, 다음 범위까지만 사용한다.

- 트래픽 흐름을 다시 검토해 불필요한 Load Balancer와 VPN 계층 제거
- 과도하게 분할된 서버를 역할 기준으로 통합
- 이후 Kubernetes 통합이 가능한 단순한 토폴로지의 기반 마련

### 성장과 비용 결과

검수 서비스 사용금액은 차량의 톤당 단가를 기준으로 계산한다.

- 2024년 12월 월 검수 서비스 사용금액: 약 200만 원
- 2026년 6월 월 검수 서비스 사용금액: 약 1억 5,000만 원
- 사용금액 증가: 약 75배
- 톤당 단가는 소폭 증가했지만 구체적인 단가와 인상 폭은 업무상 비밀
- 단가 정보는 문서와 사이트에 적지 않고 필요하면 면접에서 구두 설명

월 차량 촬영·검수 수:

- 2024년 12월: 478대
- 2026년 6월: 7,555대
- 증가: 약 15.8배
- 한 달 추가 처리량: 7,077대

공개 문장에서 사용금액이 75배 증가한 원인을 전부 차량 수 증가로만 표현하면 안 된다. 차량 수 증가 외에 차량별 중량과 비공개 단가 변화가 함께 반영될 수 있다.

현재 확인된 가장 강한 성과 구조:

> 월 차량 검수 수가 478대에서 7,555대로 늘어나는 동안 클라우드 비용은 5,524만 원에서 1,052만 원으로 약 80.9% 낮아졌다.

기술적 비용 구조를 강조할 때 사용할 수 있는 별도 방향:

> 계약 보전용 임시 자원을 제외하면 실제 서비스 운영에 필요한 월 인프라 비용은 500만 원 미만으로 줄었다.

두 수치의 최종 공개 여부를 각각 확인한 뒤 사용한다.

## 현장 자동화 전후

### 자동화 이전

1. 차량이 검수 구역에 진입한다.
2. 외부 검수원이 내부 담당자에게 무전으로 준비 완료를 알린다.
3. 내부 담당자가 화면의 검수 시작 버튼을 누른다.
4. 내부 담당자가 외부 검수원과 그래플 기사에게 다시 무전한다.
5. 현장 작업을 시작한다.
6. 종료 상태도 사람이 처리해야 했다.

### 자동화 이후

1. 차량이 들어오면 시스템이 자동으로 `검수 중` 상태로 전환한다.
2. 그래플 작업기사가 거치대 태블릿의 FOMO Operator 화면에서 작업 상태를 확인한다.
3. AIA가 카메라 영상에서 차량 미검출을 연속으로 확인하면 출차 후보로 판단하고 작업을 중단한다.
4. 오판이거나 차량이 다시 들어오면 AID가 같은 차량을 복구해 기존 검수를 이어간다.

자동화한 종료는 중앙 사무실 담당자가 눌러야 했던 촬영·검수 작업의 종료다. 최종 판정 확정과는 구분한다. 소스에서 `차량 미검출 → 0140 일시정지 → 복구 가능 상태`까지는 자동으로 처리되지만, 최종 `0075 검수완료`는 검수원이 등급을 확인하고 AIM의 `send_grade`를 호출할 때 저장된다.

효과:

- 외부 검수원과 내부 담당자 사이의 무전 제거
- 내부 담당자의 반복적인 시작·종료 화면 조작 제거
- 내부 담당자가 차량 증가에 따른 병목이 되는 문제 완화
- 중앙 사무실 담당자가 하루 종일 무전을 받고 다시 현장에 작업 지시를 전달하던 반복 업무 제거
- 수동 시작이 늦어지거나 차량을 놓쳐 검수가 누락될 수 있던 운영 오류 감소
- 운영상 차량의 진입과 이탈만으로 자동 검수 흐름이 진행됨
- CEO 지시로 전사 적용 결정
- 현재 하차지 8곳에서 자동 검수가 운영 중

CEO의 전사 적용 지시 시점은 사용자가 정확히 기억하지 못한다. `aimos-aid` 저장소의 최초 커밋은 2025년 11월 11일이므로, 저장소 이력상 자동 검수 개발이 확인되는 시점은 2025년 11월이다. 이 날짜를 CEO 지시일로 단정하지 않는다.

차량당 처리 시간이나 개입 횟수에 대한 정확한 측정값은 현재 없다. 공개 문장에서는 정량 수치를 만들지 않고, 다음과 같은 운영 변화로 설명한다.

> 이전에는 현장 검수원이 중앙 사무실에 무전하고, 사무실 담당자가 검수를 시작한 뒤 검수원과 그래플 작업자에게 다시 작업 지시를 전달해야 했다. 작업이 끝나면 사무실에서 종료 상태도 직접 처리했다. 이 절차를 하루 종일 반복하면서 시작이 늦어지거나 차량을 놓치는 일이 발생했다. 자동화 이후에는 번호판 인식으로 검수를 시작하고 카메라의 차량 미검출을 바탕으로 이탈을 처리해, 무전 중계와 반복적인 수동 상태 전환을 없앴다.

관련 모바일 프로젝트는 `aimos-fomo`다.

## 사용자의 직접 기여

사용자가 직접 만들었다고 확인한 범위:

- `AIM`, `aimos-aia`, `aimos-aib`, `aimos-fofe`, `aimos-fomo`, `aimos-ais`, `aimos-aid` 전체 시스템의 설계와 핵심 구현
- 자동 검수 시작 프로세스
- 카메라 차량 추적
- YOLOX 기반 번호판 Detection 모델의 데이터 구성, 라벨링, 학습과 평가
- LPRNet을 현장 번호판 OCR에 맞게 개조하고 학습·평가
- `aimos-stitch`의 SAM 적용과 세그멘테이션 라벨링 흐름
- `aimos-venus` 학습 플랫폼을 통한 모델 학습 실행
- 방통차·그래플 DETR 학습 데이터 라벨링과 구성
- 방통차·그래플 DETR 모델 직접 학습과 평가
- 방통차·그래플 DETR 모델의 서비스 통합, 운영 임계값 설정과 현장 검증
- 한 컴퓨터에서 여러 카메라를 처리하는 비동기·스레드 로직
- `aimos-aia`, `aimos-aid`, `aimos-ais`로 이어지는 자동화 흐름

### AI 모델과 학습 파이프라인 직접 기여

사용자가 직접 수행했다고 확인한 범위:

- 현장 데이터 수집과 학습 데이터 선별
- 라벨링과 데이터셋 구성
- 학습 실행
- 결과 평가와 현장 적용 판단
- YOLOX 기반 번호판 Detection 모델
- 개조한 LPRNet 기반 번호판 OCR 모델
- `aimos-stitch`에 SAM을 적용한 세그멘테이션 작업

방통차·그래플 Detection은 사용자가 직접 학습한 모델로 표현한다.

- 방통차와 그래플을 한 이미지에서 각각 별도 클래스로 직접 Bounding Box 라벨링
- 원거리·부분 가림 객체도 식별 가능하면 라벨에 포함
- 실제 현장 이미지로 학습 데이터를 구성하고 DETR 계열 모델을 직접 학습·평가
- `aimos-venus`를 이용해 학습을 실행하고 결과를 관리
- 실제 기여는 DETR 모델을 자동 검수 시스템에 통합하고 운영 기준을 결정한 것
- Recall을 우선해 confidence threshold 0.25를 사용하고 실제 현장 영상으로 동작을 검증
- 운영 데이터가 쌓이면서 성능이 개선되는 과정을 제품·현장 관점에서 확인
- 공개 문장에서는 `방통차·그래플 학습 데이터를 직접 라벨링하고 DETR 모델의 학습·평가·검수 파이프라인 통합과 Recall 중심 현장 검증까지 담당했다`고 표현

`aimos-venus` 소스에서 확인되는 학습 플랫폼 구조:

- `aimos-venus-front`: 데이터셋과 학습 요청, 학습 상세·이력·자원 현황을 다루는 Flutter UI
- `aimos-venus-backend`: gRPC API, 학습 명령 발행, 상태 이벤트 수신과 학습 이력 저장
- `aimos-venus-agent`: Runner 컴퓨터에서 실제 학습을 실행하고 자원 상태를 수집
- 데이터셋 버전, 클래스와 라벨 통계, Train/Validation/Test 경로 관리
- 학습 실행·취소·강제 종료, 로그, 모델 산출물과 실행 이력 관리
- GPU·CPU·RAM·디스크 자원 현황 수집과 조회

이 구조를 근거로 사용자의 기여는 반복 가능한 학습 실행 환경을 직접 구축하고, 그 위에서 방통차·그래플 DETR 모델을 직접 학습·평가한 경험으로 설명할 수 있다. OCR·번호판 Detection과 SAM도 별도 확인 결과에 따라 직접 기여로 사용한다.

#### AIMOS Venus: 모델 뷰어에서 학습 운영 플랫폼으로

개발 동기:

- LPRNet을 반복해서 개조하면서 checkpoint의 모델 구조와 layer별 weight 크기를 한눈에 보고 싶었음
- weight shape와 분포를 보면 학습 개선에 도움이 될 것으로 기대
- 실제로 만들어 사용해보니 weight 정보만으로 학습 방향을 판단하는 기능은 효용이 크지 않았음
- 모델 실험보다 더 큰 병목은 로컬 폴더로 흩어진 데이터셋, 실행 설정, weight와 결과물을 일일이 관리하는 일이었음
- 이에 Venus의 중심이 모델 구조 시각화에서 데이터셋·학습 실행·실험 이력 관리로 확장됨

현재 모델 분석 기능의 실제 범위:

- 학습 산출물에서 PyTorch `.pt`, `.pth` checkpoint를 최대 16개 선택
- checkpoint의 `state_dict`에서 최대 96개 layer를 추출
- 이름과 tensor 차원을 바탕으로 Conv1D/2D/3D, Linear, Embedding, Normalization 등을 추정
- layer별 weight shape, parameter 수, mean absolute value, RMS, standard deviation, max absolute value와 zero ratio 계산
- Frontend에서 layer 이름을 그룹으로 묶어 구조도와 checkpoint별 변화로 표시
- layer 또는 전체 checkpoint 정보를 AI 질문용 prompt로 복사 가능

이 기능의 한계:

- 실제 모델 코드를 실행해 forward graph를 그리는 구조 분석기가 아님
- residual branch, activation shape, 실제 연결 관계를 확정할 수 없음
- checkpoint의 이름·shape·weight 통계로 추정한 시각화임
- 사용자의 실제 평가도 학습 개선에 직접적인 도움은 제한적이었다는 것
- 포트폴리오에서는 모델 구조 뷰어를 Venus의 대표 성과로 내세우지 않음

현재 데이터셋 관리 흐름:

1. Frontend에서 Runner 컴퓨터의 허용된 로컬 경로를 탐색한다.
2. 업로드 전에 파일 수·용량·이미지 수·해상도·읽기 실패와 제외 항목을 빠르게 스캔한다.
3. 필요하면 업로드 과정에서 이미지 크기를 통일한다.
4. Runner가 전체 파일의 hash와 dataset manifest를 만든다.
5. YOLO·COCO 구조를 분석해 이미지·라벨·클래스·Train/Validation/Test split, 미라벨 이미지, orphan label과 잘못된 파일을 집계한다.
6. 사용자가 분석 결과와 자동 생성된 변경 요약을 확인한 뒤 업로드를 승인한다.
7. Runner가 Presigned URL로 Object Storage에 직접 업로드한다.
8. Backend가 manifest와 checksum을 검증하고 새 dataset version을 등록한다.

데이터셋 버전 관리에서 확인한 기능:

- Dataset Group과 Dataset 단위 분류·검색
- 전체 snapshot 또는 기존 버전에 변경분을 합치는 patch version
- parent version, revision number와 version note 기록
- SHA-256 content addressing을 이용한 동일 파일 재사용
- 변경된 오브젝트만 전송해 대용량 데이터 재업로드 최소화
- 추가·삭제·수정·이름 변경 파일과 class 증감 비교
- class 분포, 이미지 해상도와 split 통계 저장
- manifest와 분석 보고서 보관
- 어떤 dataset version과 subpath가 어느 학습 실행에 쓰였는지 usage 이력 저장
- 한 번 사용된 버전을 학습 기록과 연결해 재현 가능한 입력으로 관리

학습 실행 흐름:

1. 사용자가 Project, Git repository, branch, Runner와 Python entrypoint를 선택한다.
2. Args에 사용할 dataset version·layout·subpath와 model asset version을 연결한다.
3. Backend가 설정과 dataset usage를 TrainRecord에 저장하고 RabbitMQ로 실행 명령을 발행한다.
4. Agent가 지정한 Git revision을 준비하고 `.python-version`, `pyproject.toml`, `uv.lock`을 기준으로 실행 환경을 만든다.
5. NVIDIA 환경은 Docker, Apple Silicon은 native MPS 경로로 학습을 실행한다.
6. dataset과 model input을 Runner cache에 내려받고 실행 Args의 경로를 실제 로컬 경로로 치환한다.
7. 학습 코드의 `VENUS_METRIC` 출력을 loss·accuracy 등의 시계열로 수집한다.
8. checkpoint·이미지·결과 파일을 artifact로 Object Storage에 올리고 실행 이력과 연결한다.

실행 운영 기능:

- Runner별 대기열과 자원 예약을 고려한 배차
- 학습 실행·취소·강제 종료
- CUDA OOM 감지 후 batch size를 줄여 재시도하는 경로
- GPU·CPU·RAM·디스크 사용량을 Prometheus·Mimir로 수집하고 5초 주기로 표시
- 학습·Agent 로그를 Loki에서 조회하고 검색·레벨 필터·실시간 동기화
- 실행별 metric graph와 여러 실행 비교
- Git commit, command, Args, dataset version, model input, 결과와 오류 기록
- artifact 파일 목록·이미지 미리보기·전체 다운로드
- Agent 재시작 후 완료 이벤트 재전송과 중복 command 실행 방지

세 저장소의 책임:

- `aimos-venus-front`: Flutter UI. 데이터셋 업로드·버전 분석, 학습 요청, 실행 비교, 로그·자원·artifact·model analysis 표시
- `aimos-venus-backend`: gRPC API, DB 실행·데이터셋 이력, Object Storage Presigned URL, RabbitMQ 명령·상태 중계, Mimir·Loki 조회
- `aimos-venus-agent`: Runner에서 파일 스캔·dataset 분석·업로드, Git·실행 환경 준비, 학습 프로세스 제어, metric·artifact·model analysis 수집

사용자의 직접 기여와 실제 사용 범위:

- `aimos-venus-front`, `aimos-venus-backend`, `aimos-venus-agent` 전체를 사용자 혼자 설계·구현
- 실제 Venus에서 학습한 모델은 번호판 OCR, 번호판 Detection, 방통차 Detection, 그래플 Detection
- 그 밖의 모델까지 운영했다고 확장하지 않음
- 팀 단위로 사용하는 내부 MLOps 플랫폼
- 사용자가 전체 구조를 설계하고 Frontend·Backend·Agent를 단독 구현
- 번호판 OCR, 번호판 Detection, 방통차 Detection, 그래플 Detection의 데이터셋·학습 실행·결과 관리에 사용

현재 소스 기준 Venus의 핵심 정의:

> 로컬 폴더와 수동 명령으로 흩어지던 데이터셋·학습 설정·실행 결과를 버전이 연결된 하나의 흐름으로 만든 내부 MLOps 플랫폼. 데이터셋을 내용 기반으로 버전 관리하고, 원격 Runner에서 동일한 Git·환경·입력으로 학습을 실행하며, metric·로그·자원·checkpoint를 실행 이력에 함께 남긴다.

공개 문장 후보:

> 번호판 OCR을 반복 개선하면서 데이터셋과 실행 설정, weight 파일이 로컬 폴더에 흩어지는 문제가 더 큰 병목이라는 것을 발견했다. 이를 해결하기 위해 Flutter·Go·RabbitMQ 기반의 학습 운영 플랫폼 AIMOS Venus를 만들었다. 로컬 Runner의 데이터셋을 분석해 Object Storage에 내용 기반 버전으로 저장하고, Git revision·dataset version·model input을 고정한 상태에서 학습을 실행해 metric·로그·자원·artifact까지 하나의 실행 이력으로 연결했다.

정량 성과는 아직 확인하지 않았다. 사용 인원, 관리한 데이터셋·모델·학습 실행 수를 확인하기 전에는 시간 절감률이나 생산성 향상 수치를 만들지 않는다.

현재 Venus는 팀 단위로 사용하는 내부 MLOps 플랫폼이다. 사용자가 전체 구조를 설계하고 Frontend·Backend·Agent를 단독 구현했으며, 네 종류의 실제 모델에 대한 데이터셋 버전 관리, 원격 학습 실행, metric·로그·자원·artifact 관리를 하나의 흐름으로 제공한다.

아직 확인하지 않은 항목:

- 모델별 학습 이미지 수와 라벨 수
- Train/Validation/Test 분할 기준
- 모델별 세부 버전 변천
- 평가 지표와 운영 적용 기준
- 현장 오탐·미탐을 줄인 구체적인 데이터 개선 사례

#### AIMOS Stitch: 개인용 세그멘테이션 도구에서 팀 라벨링 플랫폼으로

개발 배경:

- 최초에는 철스크랩 세그멘테이션 라벨링을 위해 개발을 시작
- 이후 사용자가 번호판·Detection 등 자신의 모델 학습 데이터를 직접 라벨링하는 데 필요한 기능을 추가
- 기존에는 원본 파일을 로컬 컴퓨터로 내려받고 작업한 뒤 다시 업로드·이동·취합하는 과정이 반복됨
- 파일이 작업자 컴퓨터와 여러 저장 위치로 복제되는 구조 때문에 보안과 데이터 이력 관리 문제가 발생
- 이를 통제하기 위해 보안 솔루션 `클라우디움`을 도입. 설치와 초기 구축 등에 약 1억 원의 일회성 비용 발생
- 이후 기술지원·유지보수 성격의 SM 비용이 매년 발생. 기억상 연간 약 1,800만~3,000만 원이며 정확한 계약금액은 미확인
- 외부 라벨링 도구 `Superstar`도 연간 약 2천만 원에 구매해 사용
- 사용자는 기존 도구를 계속 구매하고 복잡한 보안 절차를 덧붙이기보다, 실제 사내 데이터 흐름에 맞는 플랫폼을 직접 만들겠다고 제안
- 핵심 방향은 파일을 사람마다 로컬로 전달하는 방식에서 클라우드의 원본을 중심으로 권한·분배·작업·검수를 연결하는 방식으로 바꾸는 것
- 저장소 이력에서 확인되는 최초 Flutter 구현은 2025년 12월이며, 철스크랩 카테고리를 폴리곤으로 구분하는 데스크톱 세그멘테이션 도구였음
- 최초 버전은 이미지 한 장을 불러와 폴리곤을 그리고 로컬 JSON으로 프로젝트를 저장하며 COCO·YOLO·Mask 형식으로 내보내는 구조
- 이후 개인용 로컬 도구를 팀·프로젝트 단위의 데이터 배분, 라벨링, 검수와 export를 연결하는 플랫폼으로 확장

2026년 7월 사내 발표자료에서 확인되는 기존 업무 흐름:

1. 포털에서 라벨링 대상을 선정한다.
2. 파일이 생성되기를 기다린다.
3. 파일을 로컬로 다운로드한다.
4. 클라우디움에 자료를 다시 업로드한다.
5. 작업자별 폴더를 만든다.
6. 작업 파일을 각 폴더로 이동한다.
7. 작업자가 완료 사실을 보고한다.
8. 결과를 수동으로 취합한다.

자료에는 포털, NAS, 클라우디움, 구글시트와 별도 통계 프로그램으로 업무가 분산된 것으로 표현돼 있다. 기존 비교 대상으로 LabelMe, CVAT와 Superstar가 언급된다.

Stitch로 정리한 업무 흐름:

1. 포털에서 대상을 선정해 Stitch로 전송한다.
2. Stitch 안에서 데이터를 분배하고 라벨링한다.
3. Stitch 안에서 결과를 검수한다.

완료 데이터는 버튼으로 S3에 필요한 포맷으로 저장하는 방향이다. 따라서 Stitch의 핵심 개선은 라벨링 캔버스 하나를 새로 만든 것이 아니라, 다운로드·재업로드·폴더 배분·완료 보고·수동 취합으로 흩어진 업무를 클라우드 중심의 단일 흐름으로 바꾼 것이다.

비용 표현 주의:

- `클라우디움`은 설치·초기 구축 등에 약 1억 원의 일회성 비용이 들었고, 별도로 연간 SM 비용이 발생
- `클라우디움`의 연간 SM 비용은 기억상 약 1,800만~3,000만 원으로 정확한 금액은 미확인
- `Superstar`는 연간 약 2천만 원의 상용 라벨링 도구
- 이력서 공개 시점에는 `클라우디움`과 `Superstar` 계약이 종료되고 Stitch로 완전히 대체된 상태로 표현
- `초기 구축비 약 1억 원과 지속적인 유지보수비가 필요한 보안 솔루션, 연간 약 2천만 원의 상용 라벨링 도구에 의존하던 흐름을 자체 플랫폼으로 대체`라고 표현
- 이미 지출한 클라우디움 초기 구축비 1억 원은 절감액에 포함하지 않음
- 정확한 SM 계약금액이 확인되기 전에는 연간 총 절감액을 특정하지 않음

현재 제품 구조:

- `aimos-stitch`: Flutter 기반 desktop-first 라벨링 클라이언트
- `aimos-stitch-backend`: Go 기반 gRPC API와 프로젝트·작업·검수·스토리지 메타데이터 관리
- Flutter Frontend와 Go Backend의 전체 구조를 사용자가 혼자 설계·구현
- 팀, 프로젝트, 멤버, 그룹과 역할별 권한을 기준으로 작업 범위 분리
- 로컬 파일과 Object Storage의 이미지를 같은 작업 화면에서 처리
- 프로젝트 단위로 이미지와 메타데이터를 검색·분배하고 작업자에게 할당
- 이미지 한 장을 하나의 Task 단위로 관리하며 작업 시작, 완료, 검수, 반려와 재작업 상태를 기록
- 프로젝트별 카테고리, 라벨링 가이드, 마감일, 멤버별 작업량과 클래스 통계 관리

도입 규모:

- 현재 약 30명 규모의 사용자 팀을 대상으로 POC 진행 중
- 해당 팀만으로도 연간 수만 장의 이미지 라벨링이 발생할 것으로 예상
- `연간 수만 장`은 현재까지 처리한 누적 실적이 아니라 POC 이후의 예상 처리 규모
- 이력서에는 `약 30명 규모의 팀을 대상으로 POC를 진행하고, 연간 수만 장 규모의 라벨링 업무를 수용하도록 설계`라고 표현

POC 검증 방향:

- 현재 POC에서 가장 중요하게 보는 영역은 라벨링 운영 통계
- 운영 통계는 아래 네 영역을 모두 포함하며 현재 기능에 반영 중
  - 작업자별 처리 이미지 수와 평균 작업시간
  - 클래스별 라벨 수와 데이터 분포
  - 검수 승인·반려율과 재작업 현황
  - 프로젝트 전체 진행률과 상태별 작업 수
- 최종 이력서에는 네 영역을 모두 제공하는 팀 운영 통계 기능으로 표현
- 라벨링 작업 속도는 기존 도구보다 좋아진 것으로 이미 내부 확인
- 정확한 전후 시간과 개선율은 아직 기록하지 않았으므로 정량 수치는 만들지 않음
- 오토 라벨링 설정은 선택된 회원사에 해당하는 `Team` 범위로 격리
- 관리자가 회원사별 AI endpoint와 디텍션 모델 목록을 관리하고, 모델 예측을 작업자가 수정·검수해 승인 데이터로 전환하는 흐름

라벨링 편집 기능:

- 자유 폴리곤, 사각형, 원·타원 작성
- 점 단위 편집, 다중 선택, 이동, 복사·붙여넣기, 삭제와 Undo·Redo
- 폴리곤 합치기와 지우개 기반 영역 제거
- 카테고리 지정·필터, 최근 카테고리 단축키와 사용자별 단축키 설정
- 이미지 이동, 확대·축소, 화면 맞춤, 선택 영역 확대와 인접 이미지 prefetch
- 작업 이력 checkpoint와 이전 상태 미리보기
- 이미지 위 위치 기반 댓글, 실시간 댓글 thread와 작업 알림

SAM 보조 세그멘테이션 흐름:

1. Backend가 encoder·decoder 모델 manifest와 Presigned 다운로드 URL을 제공한다.
2. 클라이언트가 모델 파일의 크기와 SHA-256을 검증해 로컬에 보관한다.
3. Flutter 클라이언트가 ONNX Runtime으로 encoder·decoder를 직접 실행한다.
4. 한 이미지의 embedding을 계산해 cache하고, 같은 이미지에서 반복 prompt 시 재사용한다.
5. 사용자가 positive·negative point 또는 Bounding Box prompt를 입력한다.
6. 여러 mask 후보를 score와 함께 받아 순환 선택한다.
7. 선택한 mask contour를 폴리곤으로 변환해 기존 라벨링 편집 흐름에 추가한다.

SAM을 서버 호출에만 의존하지 않고 클라이언트 로컬 추론으로 구성해, 이미지마다 반복되는 encoder 계산은 embedding cache로 줄이고 point·box prompt에 대한 decoder 결과를 즉시 편집 가능한 폴리곤으로 연결한다.

모델 보조 기능:

- Stitch 화면의 `회원사`는 Backend의 `Team`과 대응하며 AI 설정과 모델 목록은 `team_id`로 분리
- 관리자는 회원사별 AI 설정 화면에서 디텍션 기본 URL과 세그멘테이션·분류 서버 URL을 설정
- 디텍션은 한 회원사 안에 여러 모델을 이름과 전체 endpoint URL로 등록·수정·삭제 가능
- 프로젝트 설정에서 해당 회원사의 디텍션 목록 중 사용할 모델 하나를 선택
- 추론 시 Backend가 `project_id + team_id`로 프로젝트에 연결된 모델 URL을 확인한 뒤 Presigned image URL과 threshold를 모델 서버에 전달
- 디텍션 응답의 `class_id`를 프로젝트 category detail의 `category_index`와 대응시켜 라벨 초안의 클래스로 사용
- 세그멘테이션·분류는 현재 회원사별 공용 endpoint 하나를 사용하며 프로젝트별 모델 registry 선택 구조는 아님
- 모델 API 인증정보를 관리자 화면에서 모델마다 입력하는 구조는 아님
- 분배 Backend 인증은 인프라 mTLS로 처리하고, 세그멘테이션 외부 API 인증은 서버 환경의 Keycloak client credentials로 처리
- 설정 화면에서 함께 관리하는 연동값은 클라우드 Backend base URL, 이미지 CDN host, 데이터셋 생성 sub URL
- 현재 이미지에 Detection을 실행하고 confidence threshold 이상 후보만 표시
- 후보를 확인한 뒤 현재 작업 폴리곤으로 일괄 적용
- Segmentation과 Classification 결과를 함께 받아 category가 지정된 폴리곤으로 적용
- AI 결과도 일반 폴리곤과 동일하게 수정·삭제·Undo할 수 있어 자동 예측을 최종 정답으로 고정하지 않음

대용량·클라우드 이미지 처리:

- 다음 로컬·클라우드 이미지를 미리 가져오는 prefetch 흐름
- decode한 이미지의 lease와 만료를 관리하는 메모리 cache
- 클라우드 원본 이미지를 로컬에 암호화해 보관하는 cache
- 원본은 Presigned URL, 목록 탐색은 별도 thumbnail과 CDN 경로를 사용
- Object Storage credential은 Backend에서 암호화해 저장
- 프로젝트별 스토리지 대상과 검색 필드를 설정하고 외부 시스템에서 들어온 이미지·메타데이터를 같은 프로젝트 흐름에 연결

검수와 데이터셋 산출:

- 작업 완료 후 검수자가 승인하거나 반려해 재작업 상태로 전환
- 검수 결과 취소, 코멘트와 작업 알림 제공
- 프로젝트 상태·작업자·클래스 기준 통계 제공
- Stitch JSON, COCO, YOLO, Pascal VOC 형식의 데이터셋 export 지원
- COCO는 polygon 또는 RLE annotation 방식을 선택 가능
- 클라우드 데이터셋 ZIP 생성은 NCP Cloud Function으로 분리하고 결과를 Object Storage에 저장

현재 소스 기준 Stitch의 핵심 정의:

> 모델 예측을 그대로 정답으로 쓰는 도구가 아니라, 대량의 현장 이미지를 팀에 분배하고 사람이 AI 보조 결과를 수정·검수한 뒤 학습 가능한 데이터셋으로 다시 내보내는 라벨링 운영 플랫폼. 클라우드 이미지 탐색, 로컬 SAM 추론, 폴리곤 편집, 작업 이력과 검수 상태를 하나의 프로젝트 흐름으로 연결한다.

공개 문장 후보:

> 현장 학습 데이터를 라벨링하려면 원본 파일을 로컬로 내려받아 보안 솔루션에 다시 올리고, 작업자별 폴더로 나눈 뒤 완료 파일을 수동 취합해야 했다. 이 분산된 흐름을 바꾸기 위해 Flutter Frontend와 Go Backend로 구성된 AIMOS Stitch를 단독 설계·구현했다. 클라우드의 원본을 프로젝트와 작업자별로 분배하고, ONNX Runtime 기반 SAM 결과를 편집 가능한 폴리곤으로 연결했다. 작업·검수·재작업과 COCO·YOLO·Pascal VOC export까지 하나의 흐름으로 통합했다.

POC 공개 문장 후보:

> 약 30명 규모의 팀이 연간 수만 장의 이미지를 처리할 수 있도록 Stitch POC를 진행했다. 기존 도구보다 빠른 라벨링 작업성을 확인하고, 작업자별 처리량·평균 작업시간, 클래스별 라벨 분포, 승인·반려율, 프로젝트 진행률을 제공하는 운영 통계를 구축해 팀 단위 데이터 생산 과정을 가시화했다. 회원사별로 AI endpoint와 디텍션 모델 registry를 분리하고 프로젝트가 사용할 모델을 선택하게 해, 모델 결과를 라벨링 초안으로 생성한 뒤 작업자가 수정·검수해 승인 데이터로 전환하도록 구성했다.

아직 확인하지 않은 항목:

- `클라우디움`의 연간 SM 계약금액: 약 1,800만 원인지 3,000만 원인지 미확인
- `Superstar`의 정확한 라이선스 범위
- 최초 철스크랩 세그멘테이션 데이터의 구체적인 모델·사업 목적
- 실제 사용자 수, 처리한 이미지·폴리곤 수와 작업 시간 개선 폭
- POC 종료 후 실제 참여 사용자 수와 누적 처리 이미지 수
- 현재 팀 운영 여부와 실제 검수 역할 구성

#### 방통차·그래플 Detection 학습과 운영

사용자가 방통차·그래플 학습 데이터의 Bounding Box 라벨링부터 DETR 모델 학습·평가·현장 적용까지 직접 수행한 것으로 최종 확인했다.

- 한 이미지에서 `truck`, `grapple`을 각각 별도 클래스로 라벨링
- 원거리 객체도 식별 가능하면 포함
- 부분 가림 객체도 식별 가능하면 포함
- 정확한 전체 이미지 수와 분할별 수량은 확인되지 않아 공개하지 않음
- Train/Validation/Test로 데이터를 분리해 DETR 계열 모델 학습

평가와 현장 적용 기준:

- 운영 모델은 DETR 계열
- 방통차와 그래플은 화면에서 비교적 큰 객체라 일반적인 조건에서는 탐지가 잘 되는 편이었음
- 잘못 잡는 경우보다 실제 객체를 놓치는 경우가 자동 검수 흐름에 더 치명적이어서 Recall을 우선
- confidence threshold는 0.25로 운영
- 실제 현장 영상에서 수집한 데이터로 학습했기 때문에 먼저 정량 성능 지표를 기준으로 모델을 비교
- 지표를 확인한 뒤 실제 현장 영상을 재생해 차량·그래플 미탐과 동작 안정성을 다시 검증
- 운영 과정에서 데이터가 계속 쌓이면서 재학습을 통해 모델 성능이 개선됨

공개 문장 후보:

> 실제 현장 이미지에서 방통차와 그래플을 직접 라벨링하고 DETR 모델을 학습해 자동 검수 파이프라인에 통합했다. 한 번의 미탐이 차량 이탈 판단에 영향을 줄 수 있어 confidence threshold 0.25에서 Precision보다 Recall을 우선했고, 실제 현장 영상 재생 테스트로 미탐과 동작 안정성을 확인했다.

Recall의 실제 수치와 모델 버전별 개선 폭은 아직 확인하지 않았다. 직접 학습한 사실은 사용하되 확인되지 않은 성능 수치를 만들지 않는다.

현재 `aimos-ais` 배포 코드에서 확인한 구조:

- 모델 계열: RT-DETRv2
- 실행 형식: ONNX Runtime
- 입력 크기: 640×640
- 클래스: `grapple`, `truck`
- CUDA 우선, 사용할 수 없으면 CPU로 폴백

로컬 `ultralytics` 저장소에는 별도의 YOLO11n `grapple/truck` 실험 결과도 남아 있지만, 사용자가 운영 모델은 DETR라고 확인했다. 따라서 YOLO11n 실행은 운영 모델의 근거로 사용하지 않는다.

#### 번호판 Detection·OCR 학습과 운영

사용자가 번호판 영역 검출과 문자 인식을 분리한 2단계 파이프라인을 직접 학습·구축했다.

1. YOLOX로 이미지에서 번호판 영역을 검출한다.
2. 검출된 번호판을 잘라 개조한 LPRNet에 입력한다.
3. LPRNet이 한글과 숫자 시퀀스를 인식한다.
4. 인식 결과를 차량 매칭과 자동 검수 시작에 사용한다.

번호판 Detection:

- 기반 모델: YOLOX-S
- 클래스: `plate` 단일 클래스
- 사용자가 학습 데이터 구성·라벨링·학습·평가를 직접 수행
- 초기 라벨링은 Ultralytics 생태계의 기존 번호판 인식 모델 예측 결과를 pre-label로 활용
- confidence가 높은 예측 결과만 1차 라벨로 채택
- 채택한 데이터셋 전체를 육안으로 훑어본 뒤 YOLOX 학습에 사용
- 전수 수동 Bounding Box 작성이나 무검수 자동 라벨링으로 표현하지 않음
- 사용한 Ultralytics 모델의 정확한 이름과 버전은 기억나지 않아 공개하지 않음
- 최종 Detection 성능 수치는 남아 있지 않으며, 사용자 기억상 현장 번호판은 대부분 안정적으로 검출
- 현재 `aimos-ais`에서는 640×640 입력의 ONNX 모델로 추론
- letterbox 전처리, YOLOX 출력 디코딩, NMS 후 번호판 영역을 잘라 OCR로 전달
- 현재 서비스 설정의 Detection confidence threshold는 0.9지만, 학습 당시 평가 기준과 임계값 변천은 별도 확인 필요

번호판 OCR:

- 기반 모델: LPRNet
- 작업 저장소: `/Users/uwanggood/IdeaProjects/plate_ocr`
- 사용자가 원본 LPRNet을 현장 번호판 인식에 맞게 직접 개조하고 학습·평가
- 입력 크기: 72×192
- 한글·숫자·지역명이 포함된 번호판 문자 집합과 CTC 방식 시퀀스 인식 사용
- 현재 `aimos-ais`에서는 ONNX Runtime으로 실행하며, YOLOX가 잘라낸 번호판 이미지를 입력받음

소스와 Git 이력에서 확인한 LPRNet 개조 내용:

- convolution block에 BatchNorm과 Mish 적용
- residual shortcut 추가
- 채널 중요도를 재조정하는 SE attention 추가
- 서로 다른 stage의 feature를 같은 크기로 정렬해 결합하는 multi-scale feature fusion 구성
- 기존 adaptive max pooling과 제곱값 정규화 대신 adaptive average pooling과 stage별 BatchNorm 사용
- 일반 CTC Loss 대신 어려운 샘플에 더 집중하는 Focal CTC Loss 적용
- AdamW, warm-up, cosine annealing warm restarts, early stopping 적용
- multi-GPU DDP, TF32, channels-last, `torch.compile` 기반 학습 경로 구성
- 오인식 분석, 모델·weight 비교, 데이터 정제·증강·합성 스크립트 구축

가장 효과가 컸던 구조 변경:

1. 초기·중간·후반 stage의 feature map을 각각 가져온다.
2. 마지막 feature map 크기에 맞춰 adaptive average pooling으로 정렬한다.
3. stage별 BatchNorm을 적용한다.
4. 네 feature map을 channel 방향으로 concatenate한다.
5. 1×1 convolution으로 문자 클래스 logits를 만든다.

이 구조는 마지막 layer 하나만 사용하는 대신 번호판의 획·문자 형태를 담은 서로 다른 해상도의 feature를 함께 활용한다. 성능 향상 폭을 구조별로 따로 기록한 수치는 없으므로, 공개 문장에서는 multi-scale feature fusion이 가장 효과가 컸다는 사용자의 실험 판단으로 표현한다.

실제 데이터 개선 과정:

- 처음에는 공공 번호판 데이터로 학습했으나 현장 영상에서 필요한 성능이 나오지 않음
- 모회사인 대한제강 현장의 번호판 카메라 촬영본에서 오촬영·식별 불가 이미지를 직접 제외하고 초기 실데이터 약 10,000장을 선별
- 이 초기 데이터로 번호판 YOLOX Detection 모델과 LPRNet OCR 초기 버전을 학습
- 이후 번호판 카메라 데이터를 매일 수집하고 직접 라벨링하며 현장 데이터셋을 지속 확장
- 최종 학습 데이터 구성은 합성 데이터 약 5,000장과 실데이터 약 40,000장
- 공공데이터 기반 모델은 운영 성능 근거에서 제외하고, 현장 촬영 데이터 중심 학습을 핵심으로 설명

OCR 개선 과정:

- 기존 LPRNet 적용 초기 정확도는 약 60% 수준
- `6`과 `9`처럼 형태가 비슷한 숫자를 혼동하는 사례가 있었음
- 지역명이 포함된 번호판의 오인식이 자주 보여 실제 현장 지역 번호판 데이터를 집중적으로 추가
- 기울어진 번호판에서 인식 성능이 떨어지는 문제 확인
- 현장 오인식 결과를 확인하며 데이터 정제·추가 수집·라벨링·학습을 반복
- LPRNet 구조와 loss, 학습 전략을 여러 차례 변경하며 성능 개선
- 사용자 체감상 가장 큰 개선 요인은 데이터 추가보다 LPRNet 모델 구조 변경
- 특히 여러 stage의 feature를 마지막에 결합하는 multi-scale feature fusion 적용 후 성능이 크게 좋아짐
- feature 정렬 과정에서 adaptive max pooling 대신 adaptive average pooling을 사용한 변경도 효과가 있었던 것으로 기억
- 최종 Validation 정확도 98%
- 정확도 기준은 글자 단위가 아니라 번호판 전체 문자열이 완전히 일치한 비율
- 현재 확인한 `20260731` 데이터셋에는 독립 Test split이 없음
- 98%를 별도 Test 정확도로 공개하지 않고 Validation 정확도로 표현

이 과정에서 내부 도구 두 개가 파생됐다.

- 모델 실험 결과를 기록하고 비교할 마땅한 시스템이 없어 `aimos-venus` 학습·실험 관리 플랫폼을 개발
- 매일 직접 라벨링하면서 기존 도구의 사용성과 성능 한계를 겪어 `aimos-stitch` 라벨링 도구를 개발

따라서 `aimos-venus`와 `aimos-stitch`는 별도 아이디어로 만든 제품이 아니라, 번호판 Detection·OCR을 실제 현장 수준까지 개선하는 과정에서 생긴 반복 작업과 도구 부재를 해결한 내부 제품으로 연결해 설명한다.

공개 문장 후보:

> 공공데이터로는 현장 성능이 나오지 않아 번호판 카메라 촬영본을 매일 수집·정제하고 직접 라벨링했다. 약 4만 장의 실데이터와 5천 장의 합성 데이터로 YOLOX 번호판 검출 모델과 개조한 LPRNet OCR 모델을 학습했다. residual block, SE attention, multi-scale feature fusion과 Focal CTC Loss를 적용해 전체 번호판 문자열 완전 일치 기준 Validation 정확도를 약 60%에서 98%로 높였다. 반복 실험과 라벨링의 병목을 해결하기 위해 `aimos-venus`와 `aimos-stitch`까지 직접 만들었다.

현재 확인한 데이터와 학습 코드 기준:

- 데이터셋 경로: `/Users/uwanggood/Documents/dataset/학습데이터/20260731`
- Train 이미지 17,821장, 라벨 17,821개
- Validation 이미지 1,336장, 라벨 1,336개
- Train 고유 번호판 7,469개
- Validation 고유 번호판 826개
- 파일명에서 추출한 번호판 기준 Train·Validation 중복 0개
- 별도 `test` 디렉터리는 없음
- `train_lprnet.py`의 변수명은 `test_dataset`이지만 실제로 `validation_dirs`를 읽음
- 이 Validation 결과를 best model 선택과 early stopping에도 사용
- `Settings.test_dirs` 설정과 `scripts/test_lprnet.py`는 존재하지만 현재 학습 흐름의 독립 Test split으로 연결되지 않음
- 현재 `train_lprnet.py`는 Validation 데이터셋 생성 시 `is_train=False`를 넘기지 않아 기본값상 학습 augmentation이 Validation에도 적용됨

따라서 98%는 사용자가 확인한 내부 Validation의 전체 번호판 문자열 완전 일치 정확도로만 표현한다. 독립 Test나 실서비스 정확도라고 바꾸어 쓰지 않는다. 현재 코드 그대로 재측정하면 Validation augmentation을 분리한 뒤 고정된 데이터로 다시 평가해야 한다.

운영 배포 후 실제 인식 성공률이나 오인식 건수를 별도로 집계한 자료는 없다. 공개할 때는 다음 두 사실을 결합해 운영 정확도를 추정하지 않는다.

- 전체 문자열 완전 일치 기준 Validation 정확도 98%
- 자동 검수 시스템이 현재 하차지 8곳에서 운영 중

#### STNet 제거와 데이터 Augmentation 전환

기존 LPRNet에는 기울어진 번호판을 내부에서 정렬하기 위한 Spatial Transformer Network인 STNet이 포함돼 있었다. 사용자는 STNet을 비활성화하는 데 그치지 않고 최종 구조에서 제거하고, 촬영 조건의 변화를 학습 데이터 augmentation으로 직접 노출하는 방식으로 전환했다.

현재 학습 코드에서 확인한 online augmentation:

- `RandomPerspective`: 원근과 기울어짐 변화
- `RandomAffine`: ±2도 회전, 위치 이동, 0.95~1.05 배율 변화
- `ColorJitter`: 밝기·대비·채도·색조 변화
- `GaussianBlur`: 초점 저하와 흐림 근사
- 원본 비율을 유지하는 resize와 padding

별도 데이터 증강·합성 스크립트에서 확인한 변환:

- ±5도 회전
- perspective transform
- 밝기와 대비 변화
- Gaussian noise
- color jitter
- Gaussian blur
- motion blur

명시적인 glare·bloom 합성은 현재 소스에서 확인되지 않는다. 빛번짐과 열악한 촬영 조건은 밝기·대비·색상 변화, blur와 noise 조합으로 근사했다고 설명한다.

공개 문장 후보:

> 기존 LPRNet은 `6·9`와 지역명, 기울어진 번호판에서 오인식이 반복됐다. 현장 지역 번호판 데이터를 집중적으로 보강하고 STNet 기반 내부 보정 대신 perspective·affine·조명·blur·noise augmentation으로 촬영 조건을 학습시켰다. 고정된 모델만 조정한 것이 아니라 오인식 유형을 기준으로 데이터와 구조를 함께 반복 개선했다.

사용자가 직접 구현하지 않고 방향을 정해 지시한 것으로 확인된 범위:

- Kubernetes 관련 작업
- DB 서비스 자체화 관련 작업

AI가 나중에 코드에 추가한 것으로 확인된 범위:

- PID 기반 PTZ 제어
- Kalman Filter 기반 위치 보정
- 현재의 단계별 줌·재정렬 구조 일부

포트폴리오에서는 초기 현장 문제를 발견하고 룰 기반 추적을 만든 사용자의 판단과, 이후 AI를 활용해 구조를 발전시킨 과정을 구분해서 쓴다.

## 저장소별 역할

저장소 루트: `C:\Users\thdwo\IdeaProjects`

### AIM

- `aimos-fofe`와 `aimos-fomo`가 사용하는 제품 Backend
- 두 Frontend에 핵심 업무 API 제공

### aimos-aib

- 모델 관련 계산과 처리 흐름을 담당하는 Backend
- 모델 요청과 결과 처리의 중앙 경계
- 큐를 통해 모델 서버에 다운로드·업로드 Presigned URL 전달

### aimos-aid

경로: `C:\Users\thdwo\IdeaProjects\aimos-aid`

- AIM에서 차량 정보를 수신
- 카메라와 번호판 OCR로 차량 인식·매칭
- 대기·복구 차량 상태 관리
- 매칭 후 AIA에 `edge-start` 요청
- 검수 완료·복구 webhook 처리
- 차량의 물리적 이탈을 직접 판단하지는 않음
- AIA의 완료·복구 결과에 따라 하차지 활성 슬롯과 차량 상태를 정리

### aimos-aia

경로: `C:\Users\thdwo\IdeaProjects\aimos-aia`

- Edge 시스템의 전체 작업 흐름 담당
- AID가 시작한 검수 작업 수신
- 카메라 촬영과 이미지 처리
- Object Storage 업로드
- AI 판정 파이프라인 실행
- 차량 검출 결과를 이용한 이탈 후보 판단
- 완료와 복구 결과를 AID에 전달

#### AIA·AID 자동 시작과 복구 상태 전이

소스에서 확인한 시작 흐름:

1. AID가 AIM에서 검수 대기 차량과 하차지 카메라 정보를 받는다.
2. 필요한 하차지의 카메라에서 번호판을 OCR한다.
3. 번호판 끝 4자리, 허용 하차지, 진행 중 중복 여부를 검사한다.
4. 조건이 맞으면 차량을 대기 목록에서 진행 상태로 옮기고 AIA에 `edge-start`를 보낸다.
5. AIA는 촬영·차량/그래플 검출·레이어 변화 감지·업로드·AI 판정 요청을 수행한다.

소스에서 확인한 이탈 판단:

- AID는 번호판을 이용해 차량 진입과 재진입을 매칭하며, 차량 이탈 자체를 판단하지 않는다.
- AIA의 Detection 결과에서 차량 바운딩 박스가 없으면 미검출 카운트를 올린다.
- 차량이 다시 검출되면 카운트를 0으로 초기화한다.
- 현재 기본 임계값은 연속 4회다.
- 3회째에는 카메라 위치를 다시 잡는 흐름을 시도해 일시적인 추적 상실을 완화한다.
- 4회 연속 미검출이면 `TRUCK_BLANK` 예외와 상태 코드 `0140`을 발행한다.
- 따라서 판단 기준은 번호판 재인식이나 특정 구역 통과가 아니라, 검수 카메라의 연속 차량 미검출이다.
- 프레임 간 처리 간격이 가변적이므로 이를 고정된 `몇 초 후 출차`로 표현하면 안 된다.

소스에서 확인한 복구 흐름:

1. AIA가 복구 가능한 설정에서 AID의 `edge-recover` webhook을 호출한다.
2. AID는 진행 중 슬롯을 비우되 해당 차량 정보를 삭제하지 않고 복구 대기 목록으로 이동한다.
3. 같은 차량의 번호판 끝 4자리가 허용된 하차지에서 다시 보이면 AIA에 `edge-start`를 다시 보낸다.
4. 현재 기본 설정상 복구는 최대 2회, 복구 대기는 300초다.
5. AID는 AIA의 활성 작업 목록과 20초 주기로 동기화해 한쪽에만 남은 작업도 정리·복구한다.

완료 신호의 의미:

- AID의 `edge-complete`는 물리적 차량 이탈 판단 API가 아니다.
- AIA 작업이 정상 종료되거나 외부 취소로 끝났을 때 AID에 완료를 알려 활성 슬롯과 저장 상태를 정리한다.
- `TRUCK_BLANK`는 AIA 내부에서 `출차` 결과로 기록되지만 사용자 화면 상태는 `0140`, 즉 출차에 따른 일시정지로 발행된다.
- AIB는 이 메시지를 받아 DB 진행 상태를 `0140`으로 저장하고 사용자 화면에 전달한다.
- 최종 `0075 검수완료`는 AIM의 `send_grade`에서 검수원이 최종 등급을 전송할 때 저장된다.
- 따라서 포트폴리오에서는 `차량 이탈 감지와 촬영 작업 종료 자동화`와 `검수원의 최종 판정 확정`을 분리해서 표현한다.

Git 이력상 연속 미검출 판단과 현재 AID의 자동 시작·완료·복구 흐름은 사용자 계정의 커밋으로 확인된다. 현재 더 복잡해진 PTZ PID·Kalman 구조와 섞지 않고, 사용자의 직접 구현으로 설명할 때는 이 상태 전이와 예외 복구 판단을 중심으로 사용한다.

### aimos-ais

경로: `C:\Users\thdwo\IdeaProjects\aimos-ais`

- Edge용 Detection 서버
- FastAPI 기반 모델 추론 API와 GPU 추론 실행

### aimos-fofe

- `AIM` Backend를 사용하는 운영·관리 Frontend
- 검수원과 관리자가 PC에서 사용
- 미터링·빌링, 대시보드, 사용자 관리 기능 포함

### aimos-fomo

- `AIM` Backend를 사용하는 현장 모바일 Frontend
- 일반 검수원은 모바일에서 현장 검수 화면을 사용
- 그래플 작업기사는 거치대 태블릿에서 별도의 Operator 화면을 사용
- 같은 FOMO 안에서 일반 검수 흐름과 장비 Operator 흐름이 분리됨

#### 소스에서 확인한 사용자 흐름

일반 검수원·관리자 흐름:

1. 사용자 로그인과 회원사 선택
2. 사업장·하차지·검수 상태별 차량 목록 조회
3. 차량번호, 배차번호, 사업장, 하차지, 상태, 공급업체 검색
4. 차량 상세 진입
5. 하차지, 카메라, 차종과 배차 유형 확인·선택
6. 검수 시작, 일시정지, 재시작과 종료
7. 실시간 영상과 STOMP 이벤트로 검수 상태 확인
8. AI 등급·품목 비율, 위험물 탐지와 레이어 이미지 확인
9. 필요하면 등급과 비율을 수정하고 재등급 계산
10. 최종 종합등급, 감량, 반품·경고 사유와 메모 저장

현장 Operator 흐름:

1. 그래플 작업기사가 거치대 태블릿의 별도 디바이스 계정으로 로그인
2. 선택된 하차지의 실시간 카메라와 검수 상태 확인
3. 화면에 `검수 중` 상태가 표시되면 실시간 영상을 보며 그래플 작업 시작
4. 화면 조정 상태와 그래플·차량 겹침 안내 확인
5. 위험물 탐지 알림과 이미지 확인

코드에는 Operator용 일시정지·재시작 버튼과 API 호출이 존재한다. 하지만 버튼 노출은 장비 설정과 권한 값으로 제어되며, 현재 운영 권한에서는 묶어둔 상태라 그래플 작업기사는 사용하지 않는다. 현재 Operator 화면은 작업 상태와 영상을 확인하는 읽기 전용 화면으로 본다.

추가 운영 기능:

- 차량 목록 1분 자동 갱신과 모바일 pull-to-refresh
- 차량 입차 호출
- 검수대기 차량 검색과 전환
- 하차지 변경
- 위험물 원본·판정 이미지, 바운딩 박스와 confidence 확인
- 레이어별 원본·등급·품목 이미지 확인 및 다운로드

코드 근거:

- `src/routes/index.tsx`
- `src/stores/api/index.ts`
- `src/components/VhcleInspctList/index.tsx`
- `src/components/VhcleInspctDetail/index.tsx`
- `src/components/VhcleInspctDetail/InspctInfoButtons/index.tsx`
- `src/components/VhcleInspctDetail/InspctInfoGrad/index.tsx`
- `src/components/VhcleInspctOperator/index.tsx`

FOMO가 직접 카메라 촬영을 수행한다고 표현하지 않는다. 소스에서 확인되는 역할은 실시간 영상 확인, 검수 상태 제어, AI 결과 검토와 최종 판정 저장이다.

### 전체 저장소 오너십

사용자는 다음 저장소의 구조 설계와 핵심 구현을 직접 담당했다고 확인했다.

- `AIM`
- `aimos-aia`
- `aimos-aib`
- `aimos-fofe`
- `aimos-fomo`
- `aimos-ais`
- `aimos-aid`

예외적으로 `aimos-fofe`의 일부 운영·관리 기능은 보조 개발자가 작업했고, Kubernetes와 Kubernetes 내부 데이터 서비스는 인프라 담당자가 구현했다.

전체 흐름:

`차량 진입 → 번호판 인식·차량 매칭 → 자동 검수 시작 → 촬영 → 업로드 → AI 추론 → 결과 전달 → 연속 차량 미검출 → 일시정지·종료 또는 재진입 복구`

## 현장에서 발생한 예외와 대응

### 1. PTZ 카메라 헌팅

문제:

- 모델 특성상 원거리 차량의 바운딩 박스가 프레임마다 흔들렸다.
- 박스 중심만 따라가면 PTZ 카메라가 좌우로 반복 이동했다.

초기 대응:

- 원거리에서 차량 발견
- 탐지 위치를 기준으로 차량 중심 방향으로 이동
- 줌인
- 확대된 화면에서 다시 위치 보정

### 2. 일정하지 않은 차량 위치

- 고철 재고가 쌓이는 정도에 따라 차량이 서는 위치가 달라졌다.
- 차량이 정위치가 아니라 사선으로 주차하는 경우가 있었다.
- 고정 위치 가정보다 실제 바운딩 박스와 여백을 이용하는 추적으로 발전했다.

### 3. 잘못된 이탈과 재진입

- 차량 위치를 놓치면 실제 작업 중인데도 차량이 나간 것으로 판단할 수 있었다.
- AIA는 차량 바운딩 박스가 연속 4회 없을 때 `TRUCK_BLANK`와 `0140` 상태를 발행한다.
- 중간에 차량이 다시 검출되면 미검출 카운트를 초기화하고, 임계 직전에는 카메라 재조정을 시도한다.
- 실제 출차와 추적 실패를 완전히 구분할 수 없으므로 즉시 데이터를 버리지 않는다.
- `aimos-aid`에 복구 대기 상태를 두고, 같은 차량 번호가 다시 보이면 기존 검수를 이어가게 했다.
- 기본 복구 한도는 2회, 대기 만료는 300초다.

### 4. 그래플 오인식

- 그래플이 차량 위에 있을 때 모델이 그래플을 고철로 잘못 인식할 수 있었다.
- 이 상태에서는 자동 검수를 시작하면 안 된다.
- WebSTOMP로 사용자 화면에 바운딩 박스를 표시해 작업자가 모델 판단을 직접 확인할 수 있게 했다.

### 5. GPU와 스트리밍 운영

- 현장마다 그래픽카드를 관리하기 어려웠다.
- 미디어 서버를 도입해 영상 스트리밍을 서버로 전송하는 구조를 개발 중이다.
- GPU 추론 서버 중앙화도 진행 중이다.
- 아직 완료되지 않은 작업이므로 공개 문장에서는 `개발 중`으로 명확히 표시한다.

### 6. 한 컴퓨터에서 여러 카메라 처리

- 현장별 컴퓨터 수를 늘리지 않고 한 컴퓨터에서 여러 카메라를 동시에 시작해야 했다.
- 비동기 처리와 스레드를 함께 사용해 카메라별 작업을 병렬화했다.
- 최대 동시 카메라 수와 실제 자원 사용량은 추후 확인한다.

## PTZ 추적 로직의 변화

### 사용자가 만든 초기 룰 기반 구현

Git 이력에서 초기 판단을 확인했다.

- 이미지 가로 폭 3840px 기준
- 차량 바운딩 박스의 좌·우·상·하 여백 계산
- 한쪽 여백이 임계값 이하이면 여백을 확보하는 방향으로 이동
- 차량 크기 또는 차량별 설정 비율에 따라 추적·줌 프로세스 시작
- 중앙 허용 오차 적용
- 직전 이동과 반대 방향으로 바뀌면 `gamma`를 곱해 이동량 감쇠
- 당시 `gamma` 기본값은 0.75
- 연속 최적화 최대 3회 후 다음 처리로 진행

정확한 코드 이력:

- `8baeb8c`: 좌우 여백 판정, 방향 반전 시 gamma 적용
- `3a24630`: 여백과 중앙 보정 통합, gamma 0.75, 감쇠 회복, 연속 최적화 제한

사용자의 기억 중 `같은 방향으로 여러 번 움직이면 멈춤`은 실제 구현과 약간 다르다.

- 방향 반전 시 gamma로 감쇠했다.
- 같은 방향 유지 시 감쇠를 조금씩 회복했다.
- 별도 로직에서 방향과 무관하게 연속 최적화 횟수를 최대 3회로 제한했다.

### 현재 구현

현재 `aimos-aia/src/util/camera_util.py`와 `src/const/config.py`에는 다음 구조가 있다.

- 픽셀 오차를 카메라 화각과 줌 배율에 따라 회전 각도로 변환
- PID 기반 이동량 제어
- Kalman Filter 기반 바운딩 박스 노이즈 완화
- Dead zone으로 작은 오차 무시
- 원거리 센터링 → 줌 직전 재정렬 → 줌 → 줌 후 재정렬
- 이동 후 RTSP 버퍼의 오래된 프레임을 버리고 새 프레임으로 재판단
- 최대 반복 횟수, 시간 제한, 정체 판단

이 현재 구조는 AI가 추가한 것으로 사용자 확인 완료. 사용자가 직접 처음부터 설계했다고 표현하지 않는다.

## 아직 만들지 않은 최종 공개 문장

아래는 방향만 잡은 임시 문장이다. 사실 확인이 끝나기 전 사이트에 반영하지 않는다.

> 3명 팀으로 POC를 만든 뒤 시스템은 빠르게 확장됐지만, 기능과 서버가 늘어날수록 운영 비용과 복잡도도 함께 커졌다. 3초마다 MQ로 health 정보를 보내 DB에 쌓는 모니터링, 애플리케이션 서버를 경유하는 파일 전송, 역할보다 배포 단위가 먼저 나뉜 56대의 서버를 다시 살펴봤다. 제품 API, AI 추론, 후처리, CPU 작업, 파일 전송과 관찰 영역의 책임을 다시 나누고 DB와 저장 정책을 정리했다.
>
> 동시에 번호판 인식으로 검수를 시작하고 카메라의 연속 차량 미검출을 바탕으로 이탈을 처리하는 현장 흐름을 자동화했다. 원거리 탐지에서는 바운딩 박스가 흔들려 PTZ 카메라가 좌우로 왕복했다. 차량 주변 여백으로 이동 방향을 판단하고, 방향이 반전되면 이동량을 줄이며, 연속 보정 횟수를 제한했다. 차량을 놓쳐 잘못 이탈 처리한 경우에는 같은 번호가 다시 보이면 이전 검수를 복구하도록 했다.
>
> 월 차량 검수 수가 478대에서 7,555대로 늘어나는 동안 클라우드 비용은 5,524만 원에서 1,052만 원으로 약 80.9% 낮아졌다.

## 다음 세션 시작점

다음 질문부터 이어간다.

> 3명 팀은 각각 어떤 역할이었고, 사용자는 프로젝트 리드·설계·구현·현장 대응 중 어디까지 책임졌는가?

확인 완료. 다음 질문:

> POC 성공, 약 50명 조직의 재설계, 사용자의 자동화 개발, 비용 구조 재설계는 각각 언제 어떤 순서로 진행됐는가?

확인된 시간 순서:

1. 2024년 1월: 3명 팀으로 POC 시작
2. 2024년 3월: POC 성공
3. 2024년 4월~5월 초: 약 50명 규모의 팀 구성 및 합류. 사용자 팀의 소스를 기본 베이스로 전달
4. 2024년 4월~2025년 6월: 검수 영역은 사용자 팀, 나머지 영역은 합류한 팀이 담당
5. 2025년 6월: 약 50명 규모의 팀 철수
6. 2025년 7월: 인수인계 및 최적화 시작
7. 이후 광범위한 설계 문제와 과도한 비용을 정리하며 처리 속도 개선과 비용 절감을 병행

주요 비용 절감 작업의 완료 시점은 아직 확인되지 않았다.

다음 질문:

> CEO가 자동 검수의 전사 적용을 지시한 시점과 실제 적용 범위는 어디까지인가?

확인 결과:

- CEO 지시 시점은 정확히 기억하지 못함
- `aimos-aid` 최초 커밋: 2025년 11월 11일
- 현재 하차지 8곳에서 자동 검수 운영 중
- 최초 커밋일은 개발 이력의 근거일 뿐 CEO 지시일로 사용하지 않음

다음 질문:

> 서버 수를 56대에서 10여 대로 줄였다는 수치에서, 최종 서버 수는 정확히 몇 대였고 어떤 서버를 집계한 것인가?

확인 결과:

- 최적화 이전 총 56대에는 학습용 GPU 서버 4대가 포함됨
- 현장 컴퓨터는 56대 집계에서 제외됨
- Detection과 OCR 영역을 사용자 팀이 다시 맡으며 GPU 서버를 4대에서 2대로 축소
- SageMaker 전환 후 상시 GPU 서버는 0대
- 클라우드 계약의 월 약정 금액 약 1,300만 원 중 사용하지 않는 금액을 맞추기 위해 GPU 학습 서버 1대를 임시로 추가한 예외가 있었음
- SageMaker 기반 GPU 추론 비용은 월 300만 원대
- 최종 클라우드 콘솔 화면에서 운영 중인 KVM 인스턴스는 총 10대
- 화면상 일반 Kubernetes 워커 6대, CI 워커 1대, DB 워커 3대로 보임
- 현장 컴퓨터는 이 10대에도 포함되지 않음
- Kubernetes 통합 자체는 인프라 담당자가 구현했으며 사용자 직접 구현으로 표현하지 않음

다음 질문:

> Kubernetes로 통합하기 전에 사용자가 했다는 `기초 역할`은 구체적으로 무엇인가? 서비스 분리, Docker 구성, 배포 구조, DB 통합, 트래픽 경로 설계 중 직접 결정하거나 구현한 범위를 확인한다.

확인 결과:

- 정적 Frontend를 CloudFront로 변경
- 구분·분할돼 있던 서버를 통합
- 불필요한 Load Balancer 제거
- 서비스 타입 VPN 제거
- Kubernetes 클러스터 구축은 인프라 담당자가 수행

다음 질문:

> Load Balancer와 VPN을 제거하기 전에는 요청이 어떤 경로로 돌아갔고, 제거한 뒤에는 어떤 경로로 단순화됐는가?

확인 결과:

- 정확한 기존 호출 경로와 VPN 구성 명칭은 기억하지 못함
- 비효율적인 구조를 검토해 불필요한 계층을 제거했다는 범위만 확인됨
- 공개 문장에 상세 네트워크 경로를 만들어 넣지 않음

다음 질문:

> 2025년 7월 최적화를 시작한 뒤 비용 절감 작업은 언제까지 진행됐고, 2026년 6월의 1,052만 원은 최적화가 완료된 안정 상태의 비용인가?

확인 결과:

- 2026년 6월의 1,052만 원은 최적화 한계 비용이 아님
- 기존 클라우드 계약의 월 약정 금액은 약 1,300만 원
- 약정 금액을 맞추기 위해 필요하지 않은 GPU 학습 서버 1대를 임시로 실행
- 계약 보전용 임시 자원을 제외한 실제 필수 워크로드 비용은 월 500만 원 미만
- 구조적으로는 추가 절감 가능

다음 질문:

> 실제 청구액 `5,524만 원 → 1,052만 원`과 필수 워크로드 `월 500만 원 미만`, 그리고 월 약정금액 `1,300만 원`을 공개 포트폴리오에 모두 적어도 되는가?

확인 결과:

- 실제 청구액과 필수 워크로드 비용은 공개 가능
- 월 약정금액은 공개하지 않음
- 계약 조건의 구체적인 금액 없이 임시 자원이 청구액에 포함됐다는 사실만 설명 가능

다음 질문:

> `DB 서비스 자체화`는 정확히 무엇을 의미했는가? 외부 관리형 DB를 Kubernetes 내부 DB로 옮긴 것인지, 분리된 여러 DB를 하나로 통합한 것인지 확인한다.

확인 결과:

- 관리형 서비스 타입 DB의 비용을 줄이기 위해 Kubernetes가 자체 운영하는 DB로 변경
- Redis와 RabbitMQ도 같은 이유로 관리형 서비스에서 Kubernetes 내부 운영 구조로 변경
- 전환 방향과 비용 최적화 판단은 사용자가 주도
- Kubernetes 내부 DB, Redis, RabbitMQ의 실제 구축은 인프라 담당자가 수행
- 공개 문장에서는 사용자가 직접 Kubernetes 데이터 서비스를 구축했다고 표현하지 않음

다음 질문:

> ZIP 생성과 썸네일 생성 같은 CPU 집약 작업을 보냈다고 한 `CF`의 정확한 서비스명은 무엇인가?

확인 결과:

- `CF`는 NCP Cloud Functions
- AWS의 GET 파라미터 기반 Lambda 이미지 리사이즈와 유사한 기능이 NCP에는 없어 별도 구현
- 큰 원본 이미지 업로드 시 Cloud Functions가 리사이즈 썸네일을 생성하고 CDN으로 제공
- 검출 이미지를 학습용으로 모을 때 필요한 ZIP도 애플리케이션 서버가 아닌 Cloud Functions에서 생성
- 목적은 이미지 변환과 압축의 CPU 부하를 제품 Backend API에서 분리하는 것

다음 질문:

> 썸네일과 ZIP 생성을 Cloud Functions로 분리한 뒤 Backend 서버의 CPU 사용률, 응답 지연, 장애 빈도 중 실제로 개선됐다고 확인한 것이 있는가?

확인 결과:

- 정량 측정이나 장애 감소를 확인한 자료는 없음
- 검증된 성능 개선처럼 표현하지 않음
- 제품 Backend API와 CPU 집약 작업의 실행 경계를 분리한 최적화 판단으로만 사용

다음 질문:

> 56대 구조에서 가장 먼저 손댄 문제는 무엇이었고, 실제 최적화 작업은 어떤 순서로 진행했는가?

첫 작업 확인 결과:

- 모델 응답이 MQ를 거쳐 Edge별 전용 큐와 Docker에서 동기 처리되던 구조를 가장 먼저 통합
- Edge별 처리 Docker와 이를 감시하는 별도 서버가 함께 늘어나는 문제였음
- 모델 요청과 결과 처리를 중앙 서비스로 모음
- 모델 서버에는 이미지 대신 다운로드·업로드 Presigned URL을 큐로 전달
- 현재 중앙 처리 서비스는 `aimos-aib`
- Grafana 기반 모니터링 전환을 지시하고 Slack 알림 도입

다음 질문:

> 중앙 처리 서비스 `aimos-aib`의 설계와 구현은 사용자가 직접 했는가?

확인 결과:

- `aimos-aib`의 설계와 구현은 사용자가 직접 수행
- `AIM`, `aimos-aia`, `aimos-fofe`, `aimos-fomo`, `aimos-ais`, `aimos-aid`도 사용자가 전체 구조와 핵심 구현을 담당
- `aimos-fofe`의 미터링·빌링, 대시보드, 사용자 추가 등 일부 관리 기능은 보조 개발자가 기여
- Kubernetes와 내부 DB·Redis·RabbitMQ 구축은 인프라 담당자가 기여

다음 질문:

> `AIM`은 전체 시스템에서 어떤 역할을 담당하는 서비스인가?

확인 결과:

- `AIM`: `aimos-fofe`, `aimos-fomo`의 Backend
- `aimos-aib`: 모델 관련 계산과 처리 흐름을 담당하는 Backend
- `aimos-aia`: Edge 시스템
- `aimos-aid`: 차량 자동 시작 시스템
- `aimos-ais`: Edge용 Detection 서버

다음 질문:

> `aimos-fofe`와 `aimos-fomo`는 각각 누가 어떤 상황에서 사용하는 화면인가?

확인 결과:

- `aimos-fofe`: 검수원과 관리자가 PC에서 사용하는 운영·관리 화면
- `aimos-fomo`: 작업자와 일반 검수원이 모바일에서 현장 검수할 때 사용하는 화면

다음 질문:

> FOMO에서 현장 사용자가 실제로 하는 핵심 작업은 무엇인가? 촬영, 검수 시작·종료, AI 판정 확인, 수정·승인 중 포함되는 기능을 확인한다.

소스 분석 결과:

- 차량·사업장·하차지·상태별 목록 조회와 검색
- 차량 입차 호출
- 하차지·카메라·차종·배차 유형 선택
- 검수 시작·일시정지·재시작·종료
- 실시간 영상과 STOMP 기반 상태·로그 확인
- AI 등급과 품목 비율, 위험물, 레이어 이미지 확인
- 등급·비율 수정과 재등급 계산
- 최종 등급, 감량, 반품·경고 사유와 메모 저장
- Operator 전용 디바이스 로그인과 하차지 화면
- Operator의 위험물·화면 조정·그래플 겹침 알림 확인
- 코드에는 Operator 일시정지·재시작 기능이 있으나 현재 운영에서는 권한으로 비활성화
- FOMO가 직접 촬영한다고 볼 근거는 없어 촬영 기능으로 표현하지 않음

다음 질문:

> FOMO에서 최종 등급을 수정·저장하는 검수원과 현장 Operator의 권한은 어떻게 다른가?

추가 확인:

- 현장 Operator는 일반 작업자가 아니라 그래플 작업기사
- 그래플 작업기사는 거치대 태블릿에서 FOMO Operator 화면 사용
- 일반 검수원은 모바일 검수 화면 사용
- 그래플 작업기사는 `검수 중` 상태와 실시간 화면을 보고 작업
- 일시정지·재시작 기능은 코드에 있지만 현재 운영 권한에서는 비활성화
- 최종 등급 수정과 저장은 검수원이 책임짐

다음 질문:

> 그래플 작업기사는 현재 상태·영상 확인만 하며, 최종 판정 책임은 검수원에게 있는 것이 맞는가?

확인 결과:

- 맞음
- Operator 제어 버튼은 코드상 존재하지만 현재 권한으로 묶어둠
- 그래플 작업기사는 화면의 `검수 중` 상태와 영상을 보고 현장 작업만 수행
- 최종 등급 수정과 저장은 검수원이 담당

다음 질문:

> 차량이 실제로 나갔다고 판단해 자동 완료하는 조건은 무엇인가? 카메라 추적 상실 시간, 번호판 재인식, 특정 구역 이탈 중 어떤 기준을 사용하는가?

소스 확인 결과:

- AID는 번호판 OCR로 진입과 재진입을 매칭하며 이탈을 직접 판단하지 않음
- AIA가 Detection 결과의 차량 바운딩 박스 미검출을 연속 집계
- 현재 기본값은 4회 연속 미검출이며, 다시 잡히면 0으로 초기화
- 임계 직전에는 카메라 재조정을 시도
- 임계 도달 시 `TRUCK_BLANK`, 상태 `0140` 발행
- 오판 또는 재진입이면 AID의 복구 대기 차량과 다시 매칭해 기존 검수를 재시작
- AIA의 `edge-complete`는 이탈 판정 자체가 아니라 작업 종료 후 AID 상태 정리 신호

다음 질문:

> `0140` 차량 미검출 상태가 된 뒤 최종 검수완료 `0075`로 바뀌는 것은 AIM이 자동 처리했는가, 아니면 출차·계근 데이터 같은 다른 이벤트가 처리했는가?

소스 확인 결과:

- AIB가 AIA의 `TRUCK_BLANK`를 받아 진행 상태를 `0140`으로 저장
- `0140`은 자동 검수 작업이 멈춘 상태이며 최종 판정 완료가 아님
- AIM의 `send_grade`가 검수원의 최종 등급 전송을 받아 `0075 검수완료`로 저장
- 출차 계근 데이터는 이 과정에서 함께 갱신될 수 있지만, `0075` 전환의 직접 조건은 최종 등급 전송
- 현장 자동화 성과는 중앙 사무실의 무전 중계와 수동 시작·작업 종료 제거로 표현
- 최종 등급 검토와 확정은 검수원 책임으로 유지

다음 질문:

> 차량·고철·그래플 Detection 모델은 각각 어떤 데이터로 학습했고, 사용자는 데이터 수집·라벨링·학습·평가 중 어디까지 직접 담당했는가?

확인 결과:

- 데이터 수집·선별, 라벨링, 학습, 평가와 현장 적용까지 직접 수행
- `aimos-venus`를 직접 구축하고 이 플랫폼으로 모델을 학습
- 방통차 Detection, 그래플 Detection, OCR, 번호판 Detection 모델 담당
- `aimos-stitch`에 SAM을 적용해 세그멘테이션 작업 흐름 구축
- 방통차·그래플 학습 데이터 라벨링은 직접 수행
- DETR 계열 모델 학습과 평가를 직접 수행
- DETR 모델의 자동 검수 파이프라인 통합, Recall 중심 운영 기준과 현장 영상 검증을 담당한 것으로 표현

다음 질문:

> 먼저 방통차 Detection 모델부터 보자. 학습 이미지는 어디서 수집했고, 대략 몇 장이었으며, 방통차를 어떤 기준으로 라벨링했는가?

최종 확인:

- 사용자가 방통차·그래플 DETR 모델을 직접 학습
- 데이터 규모·분할 수치는 확인되지 않아 사용하지 않음
- 방통차·그래플 Bounding Box 라벨링은 직접 수행

다음 질문:

> 방통차와 그래플은 한 이미지에서 각각 별도 클래스로 박스를 쳤는가? 가려짐·잘림·원거리 차량도 어떤 기준까지 라벨에 포함했는가?

정정:

- 한 이미지에서 방통차와 그래플을 각각 별도 클래스로 직접 라벨링
- 원거리·부분 가림 객체도 식별 가능하면 포함

다음 질문:

> 이 Detection 모델을 현장에 적용할지 판단할 때 가장 중요하게 본 지표나 테스트는 무엇이었어? mAP·Precision·Recall 같은 수치였는지, 실제 영상 재생 테스트에서 차량·그래플을 놓치지 않는지를 더 중요하게 봤는지 알려줘.

확인 결과:

- Recall을 가장 중요하게 평가
- 방통차와 그래플은 큰 객체라 일반적인 탐지는 비교적 잘 됐고, 실제 객체를 놓치지 않는 것이 더 중요했음
- confidence threshold 0.25 사용
- 현장 이미지로 만든 데이터셋이므로 우선 정량 성능 지표로 비교
- 이후 실제 현장 영상으로 재생 테스트
- 운영 데이터가 쌓이면서 모델 성능이 개선되는 과정을 확인

다음 질문:

> 방통차·그래플 Detection에 사용한 기반 모델은 처음부터 끝까지 YOLO 계열이었어? 실제 운영에 올린 주요 버전이 YOLOv5·v8·v11 중 무엇이었는지 알려줘.

확인 결과:

- YOLO가 아니라 DETR 계열
- 현재 `aimos-ais` 배포 코드는 RT-DETRv2를 ONNX Runtime으로 실행
- `grapple`, `truck` 2개 클래스
- 로컬에 남은 YOLO11n 결과는 별도 실험으로 보고 운영 근거에서 제외

다음 질문:

> 실제 운영 모델도 현재 코드에 적힌 RT-DETRv2라고 보면 돼? 아니면 처음에는 일반 DETR로 시작해서 나중에 RT-DETRv2로 교체한 거야?

최종 확인:

- 방통차·그래플 DETR 계열 모델을 사용자가 직접 학습
- 정확한 세부 버전 변천은 아직 확인하지 않음
- 현재 배포 코드는 RT-DETRv2·ONNX Runtime 구조
- 학습·평가, 서비스 통합과 운영 판단을 모두 사용자 직접 기여로 사용

다음 질문:

> OCR 모델과 번호판 Detection 모델은 네가 실제로 데이터 구성·라벨링·학습까지 직접 한 게 맞아?

확인 결과:

- 번호판 Detection은 YOLOX 기반 모델을 직접 학습
- 번호판 OCR은 LPRNet을 개조해 직접 학습
- OCR 작업 저장소는 `/Users/uwanggood/IdeaProjects/plate_ocr`
- 현재 서비스는 YOLOX가 번호판 영역을 자르고 LPRNet이 문자열을 인식하는 2단계 구조
- LPRNet Git 이력에서 residual shortcut, SE attention, multi-scale feature fusion, Focal CTC Loss 등 사용자의 개조 내역 확인

다음 질문:

> 저장소 문서대로 공공데이터 78,514장으로 pre-train하고 지역·현장 번호판 증강 데이터 9,486장으로 fine-tune한 2단계 학습이 실제 운영 OCR 모델을 만든 방식이 맞아?

정정 결과:

- 공공데이터로 먼저 학습했으나 현장 성능이 나오지 않아 최종 학습 전략에서 제외
- 대한제강 현장 번호판 카메라 촬영본에서 잘못 촬영된 데이터를 제외하고 초기 약 10,000장 선별
- 초기 약 10,000장으로 YOLOX 번호판 Detection과 LPRNet OCR 초기 버전 학습
- 이후 매일 현장 데이터를 수집·라벨링하며 데이터셋 확장
- 최종 구성은 합성 데이터 약 5,000장과 실데이터 약 40,000장
- 기존 LPRNet의 초기 정확도는 약 60%
- 반복적인 구조·학습 개선 과정에서 실험 관리 부재를 해결하려고 `aimos-venus` 개발
- 기존 라벨링 도구의 한계를 해결하려고 `aimos-stitch` 개발

다음 질문:

> 약 60%였던 초기 LPRNet을 최종적으로 어느 정도까지 올렸어? 전체 번호판 문자열이 완전히 일치해야 정답인 정확도였는지, 글자 단위 정확도였는지도 알려줘.

확인 결과:

- 최종 Validation 정확도 98%
- 번호판 전체 문자열이 완전히 일치해야 정답으로 집계
- 글자 단위 정확도가 아님

다음 질문:

소스·데이터 확인 결과:

- 현재 `20260731` 데이터셋은 Train 17,821장, Validation 1,336장
- 독립 Test split은 없음
- Train과 Validation은 번호판 단위로 분리돼 고유 번호판 중복 0개
- 학습 코드가 Validation을 `test_dataset`이라는 변수명으로 부르지만 실제 독립 Test는 아님
- 98%는 Validation 정확도로 정정

다음 질문:

> 98% 모델을 현장에 올린 뒤 별도로 확인한 실제 운영 성공률이나 오인식 건수는 남아 있어? 없다면 포트폴리오에는 Validation 98%와 현재 8개 하차지 운영 사실을 분리해서 쓰면 돼.

확인 결과:

- 운영 배포 후 실제 성공률·오인식 건수는 별도로 집계하지 않음
- Validation 정확도 98%를 운영 정확도로 표현하지 않음
- 하차지 8곳 운영 사실과 모델 정확도는 분리해 기술

다음 질문:

> LPRNet을 개선하면서 가장 자주 틀리던 유형은 뭐였어? 예를 들면 비슷한 숫자·한글 혼동, 지역명 번호판, 기울어짐, 야간·빛번짐, 작은 번호판 중 기억나는 사례가 있어?

확인 결과:

- `6`과 `9` 같은 유사 숫자 혼동
- 지역명 오인식이 자주 보여 실제 현장 지역 중심 데이터로 보강
- 기울어진 번호판 인식 저하
- 원본 LPRNet의 STNet 제거
- 기울기와 촬영 환경 대응을 augmentation으로 전환
- 현재 코드에서 perspective, affine, 밝기·대비·채도·색조, blur, noise, motion blur 확인
- 명시적인 빛번짐 합성은 없으며 조명·blur·noise 변환으로 근사

추가 소스 주의점:

- 현재 학습 코드는 Validation 데이터셋에도 기본값상 학습 augmentation을 적용
- 98%는 사용자가 확인한 내부 Validation 수치로 보존하되, 독립 Test·운영 정확도로 표현하지 않음

다음 질문:

> `6·9`와 지역명 오인식을 줄일 때 가장 효과가 컸던 건 현장 데이터 추가였어, 합성 데이터였어, 아니면 LPRNet 구조 변경이었어? 네 체감 기준으로 하나만 골라줘.

확인 결과:

- 가장 큰 개선 요인은 LPRNet 모델 구조 변경
- 특히 여러 stage feature를 마지막에 쌓아 결합하는 multi-scale feature fusion이 효과적이었음
- adaptive max pooling에서 adaptive average pooling으로 바꾼 것도 성능에 도움이 된 것으로 기억
- 구조별 ablation 수치는 남아 있지 않으므로 정량적인 기여율은 만들지 않음

다음 질문:

> 번호판 Detection YOLOX 쪽도 최종 성능 수치가 기억나? 없다면 번호판을 놓쳤던 대표 조건과 데이터로 어떻게 보강했는지만 확인하자.

확인 결과:

- 학습 시점이 오래돼 YOLOX의 최종 성능 수치는 기억나지 않음
- 번호판 Detection은 현장에서 대부분 잘 동작한 것으로 기억
- Ultralytics의 기존 번호판 인식 모델 결과로 데이터를 1차 라벨링
- 해당 pre-label을 바탕으로 YOLOX 번호판 Detection 모델 학습
- 정확한 기반 모델 이름과 버전은 확인되지 않아 쓰지 않음

다음 질문:

> Ultralytics 모델이 만든 1차 Bounding Box를 네가 직접 전부 확인·수정한 뒤 YOLOX를 학습한 거야? 아니면 confidence가 높은 결과는 그대로 쓰고 일부만 검수했어?

확인 결과:

- confidence가 높은 Bounding Box 예측만 1차 라벨로 사용
- 전체 데이터셋을 육안으로 확인
- 이 pre-label 데이터셋을 바탕으로 YOLOX 학습

다음 질문:

> 이제 `aimos-venus`로 넘어가자. 처음 Venus에서 꼭 기록하고 싶었던 건 뭐였어? 모델별 학습 설정, 데이터셋 버전, loss·accuracy 그래프, GPU 사용량, weight 파일 중 실제로 가장 답답했던 것부터 알려줘.

확인 결과:

- 최초 목적은 checkpoint를 통해 모델 구조와 layer별 weight 크기를 시각화하는 것
- weight shape와 크기가 학습 개선에 도움 될 것으로 기대했지만 실제 효용은 제한적이었음
- 더 큰 문제는 데이터셋을 로컬 폴더로 일일이 관리하는 일이었음
- 현재 Venus는 데이터셋 클라우드 버전 관리와 원격 학습 실행·실험 이력 관리가 중심
- Frontend·Backend·Agent 최신 소스를 분석해 상세 구조를 위 본문에 반영

다음 질문:

> Venus를 실제로 사용한 사람은 너 혼자였어, 아니면 팀원도 썼어? 그리고 Venus로 실제 학습한 모델이 번호판 OCR·번호판 Detection·방통차·그래플 전부인지 알려줘.

확인 결과:

- Venus Frontend·Backend·Agent 전부 사용자가 혼자 구현
- 실제 학습한 모델은 번호판 OCR, 번호판 Detection, 방통차 Detection, 그래플 Detection이 전부

다음 질문:

> 구현은 전부 네가 한 것으로 확인됐고, 실제 Venus 화면을 조작해 학습을 실행한 사용자도 너 혼자였어? 아니면 팀원이 직접 사용한 적도 있어?

확인 결과:

- Venus는 팀 단위로 사용 중인 내부 MLOps 플랫폼
- 사용자가 전체 구조 설계와 Frontend·Backend·Agent 구현을 담당

다음 질문:

> 코드 기준 회원사별 모델 설정 범위까지 확인했다. 다음은 Stitch 통계 구현이 끝난 뒤 실제 POC 결과 수치만 다시 채우면 된다.

이 프로젝트에서 추가로 확인할 항목:

- 최적화 작업의 실제 실행 순서
- 모델별 데이터 규모, 분할 방식과 평가 기준
- 멀티 카메라 최대 동시 처리 수와 자원 제약
- 비용 수치·차량 수·사용금액 중 공개 가능한 범위
- 포트폴리오에 사용할 아키텍처 다이어그램의 공개 범위

## 논문 및 포상

### 학술 논문

#### 혼재된 객체 환경에서 어텐션 기반 철스크랩 분류의 성능 향상 기법

- 학회: 2026 한국정보기술학회 하계종합학술대회
- 발표 일자: 2026년 6월 4일
- 세션: AI비전·위성영상/디지털트윈/산업안전·의사결정/법규AI
- 저자: 김우헌, 송재상, 조정록, 유민수
- 사용자 역할: 제2저자
- 소속: 아이모스(주)
- 연구 배경: 철강 스크랩 AI 검수 시스템의 분류 모델 개선 과정에서 도출한 실무 기반 연구
- 문제: Transformer 기반 분류 모델의 attention이 분류 대상이 아닌 주변 객체로 분산됨
- 접근: 비대상 영역을 검정색으로 단순 마스킹하지 않고 Gaussian Blur를 적용해 대상 객체로 attention이 집중되도록 유도
- 결과: 분류 성능 향상을 확인

사용자의 직접 기여:

- 대상 외 주변 객체의 가시성과 선명도를 조절해 학습하는 연구 아이디어 제안
- 제안한 아이디어를 바탕으로 테스트 진행
- 여러 위치에 흩어져 있던 학습 데이터 수집·제공
- 실험을 실행할 수 있도록 모델 서버 환경 정비
- 연구 아이디어뿐 아니라 실험에 필요한 데이터와 실행 환경까지 마련

공개 표기 원칙:

- 이 논문은 학술대회 발표 논문이며 학술지 게재 논문으로 표현하지 않음
- 공식 학술대회 프로그램에서 정확한 제목, 저자 순서와 발표 일자를 확인함
- 기존 이력서 PDF의 `2026.05` 메타데이터보다 공식 프로그램의 `2026.06.04`를 우선 사용
- 실험 데이터와 향상 수치가 확인되기 전까지 `성능을 크게 향상` 같은 정량적 뉘앙스는 사용하지 않음

포트폴리오용 초안:

> 철강 스크랩 분류 모델의 attention이 주변 객체로 분산되는 문제를 해결하기 위해, 대상 외 영역의 가시성과 선명도를 조절하는 학습 아이디어를 제안했습니다. 흩어진 데이터를 정리해 제공하고 모델 서버를 정비한 뒤 실험을 진행했으며, 이 결과를 연구로 정리해 2026 한국정보기술학회 하계종합학술대회에 제2저자로 발표했습니다.

확인 결과:

- 연구 아이디어 제안
- 아이디어 기반 테스트 진행
- 학습 데이터 제공
- 모델 서버 환경 정비
- 논문 원고 작성 범위와 세부 실험 수치는 아직 확인하지 않음

### 포상 이력

#### 철강 스크랩 AI 검수 POC 성과 포상

- 시기: 2024년 6월
- 형태: 사내 성과 포상금 300만 원
- 사유: 철강 스크랩 AI 검수 시스템 POC 성공과 본 사업 수주 기여
- 공개 시 `수상`보다 `사내 성과 포상`으로 표현
- AIMOS 본문에서 정리한 POC 역할과 연결하되, 회사 전체 수주를 혼자 달성한 것처럼 표현하지 않음

포트폴리오용 초안:

> 철강 스크랩 AI 검수 POC의 설계와 개발을 주도해 본 사업 전환에 기여했고, 그 성과를 인정받아 사내 포상을 받았습니다.

#### 복지로 차세대 ERP 단장배 우수개발자상

- 시기: 2021년 10월
- 프로젝트: 대국민 서비스 복지로 차세대 구축
- 기여: 메인 페이지와 공통 기능 개발, 대국민 서비스 응답 속도 40ms 이내 개선, 자동화 도구를 통한 팀 생산성 향상
- 성격: 프로젝트 단장 명의의 우수개발자상
- 외부 기관 공식 표창처럼 과장하지 않고 프로젝트 내부 수상으로 명확히 표현

포트폴리오용 초안:

> 복지로 차세대 구축에서 메인 페이지와 공통 기능을 개발하고 응답 속도를 40ms 이내로 개선했습니다. 반복 작업을 줄이는 자동화 도구까지 제작해 프로젝트 단장배 우수개발자상을 받았습니다.

### 출처 간 교차 확인

- 경력기술서 DOCX: 논문 제2저자, 연구 배경과 접근, 두 포상 명칭과 사유 확인
- 백엔드·플랫폼 엔지니어 이력서 PDF: 논문 저자 순서, 철강 스크랩 POC 포상금 300만 원, 복지로 수상 시기 확인
- 한국정보기술학회 공식 프로그램: 논문 정확한 제목, 전체 저자, 발표 일자와 세션 확인

## Independent Product 01: Esther

### 제품 정의

- 한글 업무 용어를 일관된 PostgreSQL 설계로 연결하는 local-first Flutter 데스크톱 앱
- 용어사전, 스키마 설계, 실제 DB 조회, 변경 계획 검토와 안전한 적용을 하나의 프로젝트 기록으로 관리
- 2026년 8월 14일 최초 커밋 확인
- 현재 버전 `0.4.0+5`
- Flutter/Dart 기반 Windows·macOS 데스크톱 앱
- 사용자가 제품 기획, 설계와 구현 전부 담당

### 만들게 된 실제 배경

- 같은 업무 개념인데도 데이터베이스 타입과 컬럼명이 개발자·시점마다 들쭉날쭉했음
- 팀이 공통 용어사전을 기준으로 물리명과 타입을 선택할 수 있는 구조가 필요했음
- 용어사전과 ERD를 분리하지 않고, 실제 DB 설계 흐름 안에서 함께 보며 작업하고 싶었음
- 작업 후 시간이 지나면 자신이 어떤 DDL을 실행하고 무엇을 변경했는지 기억하기 어려웠음
- DataGrip에서 작성했던 console이 사라지는 문제를 겪어 실행 이력을 별도로 보존할 필요가 있었음
- 개발 환경에 배포한 변경을 버전별로 기록하고, 이후 운영 반영 시 동일한 DDL을 다시 검토·적용할 수 있어야 했음

따라서 Esther의 핵심은 단순한 ERD 작성이 아니라 다음 세 가지를 하나로 묶는 것이다.

1. 용어사전을 통한 컬럼명·타입 일관성
2. 실행·변경 이력을 잃지 않는 DB 작업 기록
3. 개발 배포 버전을 기준으로 운영 DDL을 준비하는 migration 흐름

### 코드에서 확인한 핵심 기능

- 원자 단어 단위의 표준 영문명·축약어·타입 후보 관리
- 단어 조합을 이용한 한글명·물리명과 컬럼 타입 설계
- PostgreSQL table, column, PK, FK, index, comment 설계
- 여러 PostgreSQL 연결 프로필과 읽기 전용 schema introspection
- 기존 DB에서 용어 후보를 역추출하고 승인된 항목만 사전에 반영
- 저장된 baseline, target, live snapshot을 비교하는 3-way change planning
- drift, conflict와 파괴적 변경 위험 표시
- checksum, advisory lock, migration ledger, transaction, 사후 fingerprint 검증을 거친 개발·테스트 DB migration
- 운영·스테이징 DB에 대한 migration 실행 차단
- 전체 DDL, migration/rollback SQL, Markdown 명세와 관계도 SVG 내보내기
- SQLite 설계 원본을 private Git으로 자동 백업·동기화
- 세션, blocking 관계, Top SQL, role, WAL과 replication 상태를 읽기 전용으로 보여 주는 DBA 워크벤치
- 설계와 실제 DB를 함께 탐색하는 통합 ERD

### 안전 설계에서 드러나는 강점

- DB 자격증명을 SQLite, 로그와 설정 저장소에 남기지 않고 OS 보안 저장소에만 보관
- 운영·스테이징 변경을 제품 레벨에서 금지
- 개발·테스트 환경에서도 read-only 연결, live drift, checksum 불일치와 중복 migration을 차단
- 파괴적 변경은 migration ID를 직접 입력해야 실행
- DB 전체를 무조건 복제하지 않고 사용자가 선택해 채택한 설계만 관리 원본으로 편입
- Git 동기화 시 로컬 연결 정보와 snapshot은 제외하고 설계 데이터만 공유

### 포트폴리오 포지셔닝

단순한 ERD 편집기보다 `용어사전, 실제 DB 변경 이력과 배포 DDL을 하나의 버전으로 관리하는 로컬 우선 PostgreSQL 설계 도구`로 소개하는 편이 정확하다. 백엔드 개발자의 DB 모델링, migration 안전성, 개발 도구 UX와 데스크톱 제품 구현 역량을 한 프로젝트에서 보여 줄 수 있다.

포트폴리오용 초안:

> Esther는 같은 업무 개념이 서로 다른 컬럼명과 타입으로 설계되고, 실행했던 DDL과 배포 내용을 다시 찾기 어려운 문제에서 시작했습니다. 용어사전과 ERD를 실제 DB snapshot에 연결하고 변경 내용을 버전으로 남겨, 개발 환경에 적용한 DDL을 이후 운영 배포까지 이어 갈 수 있게 했습니다. baseline·저장 설계·live DB의 drift와 conflict를 비교하고, 검토된 변경만 transaction과 사후 검증을 거쳐 적용합니다.

확인 결과:

- 컬럼명과 타입의 일관성 문제
- 용어사전과 ERD를 함께 사용하는 설계 환경 필요
- DataGrip console 유실로 인한 실행 이력 관리 필요
- 개발 배포 내용을 버전으로 남기고 동일 DDL을 운영 반영에 사용하는 흐름 필요

## Independent Product 02: Path Doctor

GitHub의 비공개 소스 저장소 `Uwanggood/path-doctor`와 공개 바이너리 저장소 `Uwanggood/path-doctor-releases`를 확인해 실제 구현을 분석했다.

### 만들게 된 배경

- 현장 네트워크에서 속도 저하나 연결 문제가 생겨도 비개발 담당자는 패킷이 어떤 경로로 이동하는지 파악하기 어려웠음
- `traceroute` 같은 진단 명령은 일반 담당자가 직접 사용하고 결과를 해석하기 어려움
- 현장에서 서비스가 어떤 경로로 통신하고 어느 구간에서 지연되는지를 담당자가 직접 확인할 수 있는 도구가 필요했음

### 제품 정의

- Flutter로 만든 Windows 중심 데스크톱 네트워크 진단 도구
- IP, 도메인 또는 전체 URL을 입력받아 DNS, ping, route와 HTTP 응답을 한 번에 검사
- 별도 속도 검사에서 실제 download/upload Mbps를 측정
- 명령어를 아는 엔지니어용 CLI가 아니라 현장 담당자가 통신 흐름을 확인하는 시각적 도구
- 장애 원인을 자동 확정하는 제품으로 과장하지 않고, 경로와 지연 구간을 확인해 담당자와 개발자가 같은 정보를 보고 대화하도록 돕는 제품으로 설명

### 코드에서 확인한 진단 기능

- DNS lookup 성공 여부, 변환된 IP와 소요 시간
- ping 4회 결과의 평균 RTT와 packet loss
- Windows `tracert`, macOS 계열 `traceroute` 실행 및 최대 18 hop 분석
- hop별 3회 응답 시간, 평균 지연, timeout과 IP 표시
- reverse DNS와 RDAP 조회로 hop의 network·owner 정보 보강
- 사설 IP, loopback, link-local과 CGNAT 대역 구분
- 대상 URL에 대한 HTTP GET 응답 코드와 응답 시간 측정
- HTTPS가 실패하고 사용자가 scheme을 생략한 경우 HTTP로 한 번 더 확인
- Cloudflare endpoint를 이용한 download/upload 속도 측정
- 속도 검사 이력을 그래프로 표시
- 진단을 설정한 주기로 반복 실행
- 최근 진단 결과 최대 100건을 로컬 JSON으로 보존

### 단순 수치를 설명으로 바꾸는 규칙

- packet loss가 있거나 ping 평균이 150ms를 넘으면 경고
- 첫 hop이 50ms를 넘으면 PC와 공유기 사이의 로컬 구간부터 확인하도록 안내
- 이전 hop보다 80ms 이상 증가하면서 120ms를 넘는 지점을 지연 급증 구간으로 표시
- trace timeout이 있어도 ping 또는 HTTP가 정상이라면 방화벽·장비의 진단 응답 제한 가능성을 구분
- HTTP 5xx, 3초 초과 응답과 접속 실패를 각각 다른 문제로 안내
- DNS, VPN, proxy와 HTTPS 같은 용어를 일반 사용자가 이해할 수 있는 도움말로 제공

주의:

- VPN·proxy를 직접 탐지하는 기능은 아니며 진단 결과에 따른 비교·확인 방법을 안내함
- 전용 TLS handshake 분석 기능은 없고 HTTPS 요청 실패 정보로 인증서 가능성을 안내함
- 특정 hop을 장애 원인으로 확정하지 않고 다른 네트워크·핫스팟 또는 VPN 해제 상태와 비교하도록 권장함

### 경로 시각화

- 각 hop을 연결된 node 흐름으로 표시
- hop별 이름, IP, 평균 응답 시간과 응답 없음 상태 제공
- RDAP·reverse DNS를 통해 `Router`, `Provider`, `ISP edge`, `Transit`처럼 사람이 읽을 수 있는 식별 정보를 우선 표시
- 구간별 latency delta가 크면 connector와 hop을 warning 또는 critical 색상으로 강조
- 응답을 제한한 중간 장비로 판단되는 timeout hop은 숨겨 실제 응답 경로만 볼 수 있음
- 총 hop 수, timeout 수와 가장 느린 hop을 요약

### 제품화와 배포

- 비공개 저장소에서 소스를 관리하고 공개 저장소에는 installer와 manifest만 배포
- 공개 Windows `v1.0.0` installer와 GitHub Pages 제품 페이지 확인
- itch.io에도 무료 또는 후원 방식으로 공개
- 앱 내부 update manifest 확인, 플랫폼별 파일 다운로드와 SHA256 검증 구현
- Windows Store용 MSIX 설정은 존재하지만 실제 Store 공개 여부는 별도 확인 전까지 쓰지 않음
- 한국어·영어 UI 제공

포트폴리오용 초안:

> Path Doctor는 현장 담당자가 명령어 없이도 서비스의 DNS 변환, 응답 지연, packet loss, HTTP 상태와 네트워크 경로를 확인하도록 만든 데스크톱 진단 도구입니다. traceroute의 hop을 연결된 경로로 시각화하고 지연이 급증한 구간과 응답이 제한된 장비를 구분해, “서비스가 느리다”는 보고를 실제 측정 결과와 다음 확인 행동으로 바꿉니다. 별도의 download/upload 속도 검사와 반복 측정 이력도 함께 제공합니다.

확인 결과:

- hop별 IP, 이름, 응답 시간과 timeout 표시
- DNS lookup, ping RTT·packet loss, HTTP status·응답 시간 측정
- download/upload 속도 측정과 이력 그래프
- 경로 node graph와 지연 급증 구간 강조
- 진단 결과에 대한 일반 언어 설명과 다음 행동 제안
- 저장소는 로컬에 없었지만 GitHub 비공개 저장소에서 확인

## Independent Product 03: Copylight

로컬 저장소 `/Users/uwanggood/Documents/workspace/copylight`의 Flutter·Windows C++·macOS Swift 코드와 Microsoft Store 페이지를 확인했다.

### 제품 정의

- 텍스트와 이미지를 자동 수집해 검색·정리·재사용하는 local-first 데스크톱 클립보드 관리자
- Flutter UI 위에 Windows C++와 macOS Swift 네이티브 기능을 결합
- 2026년 7월 2일 최초 커밋 확인
- Windows 10·11용 제품을 Microsoft Store에 실제 공개
- 사용자가 제품 기획, 설계, 구현과 Store 배포를 전부 담당

### 만들게 된 실제 배경

- 복사했던 내용을 나중에 다시 찾아 쓰는 clipboard history가 필요했음
- 기존 clipboard 기능만으로는 저장된 내용을 바로 수정하기 어려웠음
- 자주 쓰는 내용을 pin으로 고정하고 빠르게 재사용하고 싶었음
- clip이 많아지면 검색으로 필요한 내용을 찾아야 했음
- 개발 중 복사한 JSON을 별도 도구로 옮기지 않고 바로 format할 필요가 있었음
- 필요한 기능을 하나의 작업 panel에서 해결하기 위해 직접 제품으로 구현

### 기본 사용 흐름

1. 사용자가 다른 프로그램에서 텍스트나 이미지를 복사
2. Copylight가 clipboard 변경을 감지하고 중복 여부를 확인
3. 전역 단축키로 현재 작업 화면 위에 panel을 호출
4. 검색, 그룹, pin과 label을 이용해 필요한 clip 선택
5. 선택한 clip을 원래 사용하던 프로그램에 바로 붙여넣고 panel을 닫음

### 코드에서 확인한 사용자 기능

- 텍스트와 이미지 clipboard 자동 수집
- 텍스트·label 통합 검색
- pin, 사용자 그룹, label과 drag-and-drop 순서 변경
- Ctrl 다중 선택과 Shift 범위 선택
- 여러 clip의 pin, 그룹 이동과 삭제 일괄 처리
- 전체 화면 텍스트·이미지 preview와 텍스트 편집
- keyboard selection, preview, edit와 paste 흐름
- 최근 일반 clip 100~500개 보관 설정
- pin clip은 일반 history 제한과 별도로 보존
- 같은 텍스트나 이미지가 다시 복사되면 중복 생성 대신 기존 clip을 앞으로 이동
- horizontal·vertical panel 방향과 visual theme 선택
- tray 상주, 사용자 지정 전역 단축키와 Windows 시작 프로그램 설정

### 개발자를 위한 clip 변환 기능

- JSON format, minify와 key sort
- JSON string 변환
- Markdown code block 생성과 JSON·SQL·shell 언어 추론
- URL encode/decode
- Base64 encode/decode
- JWT header·payload decode
- clip metadata의 JSON 변환

JWT 기능은 token을 검증하거나 인증하는 기능이 아니라 header와 payload를 로컬에서 decode하는 도구로 표현한다.

### 네이티브 데스크톱 통합

- Windows clipboard sequence number를 사용해 내용이 바뀌지 않았으면 payload를 다시 읽지 않음
- 650ms polling과 panel open 직전 refresh를 결합해 호출 첫 화면에 최신 clip 표시
- panel이 열린 뒤에는 polling을 멈춰 앱 자신의 copy 동작과 충돌하지 않도록 제어
- Windows C++ MethodChannel로 image clipboard read/write 구현
- Windows에서 이전 foreground window를 기억한 뒤 선택한 clip을 해당 프로그램에 직접 paste
- macOS에서도 Swift로 pasteboard image 처리와 이전 application 복귀·paste 구현
- 화면 가장자리 panel, always-on-top, taskbar 제외, focus 상실 시 자동 닫힘과 tray 제어
- 등록하려던 단축키가 충돌하면 기본 단축키로 fallback

### local-first 보안과 저장 안정성

- clip과 group metadata를 AES-GCM 256으로 암호화해 로컬 파일에 저장
- master key는 우선 OS secure storage에 저장하고, platform storage 실패 시 fallback 저장소 사용
- 이미지와 4KB 초과 텍스트는 본문 bundle에서 분리해 개별 암호화 payload로 저장
- 큰 payload의 암복호화를 isolate로 넘겨 UI 정지 방지
- image memory cache를 최대 32MB로 제한하고 LRU 방식으로 제거
- temp file 작성 후 교체하며 직전 정상 파일을 backup으로 보존
- primary 파일 손상 시 backup을 읽어 복구하고, 읽을 수 없는 원본은 별도 보존
- 변경 저장은 120ms debounce 후 수행하고 실패 시 최대 3회 자동 재시도
- 종료 전에 pending write를 모두 flush하며 저장 실패 시 종료를 취소해 데이터 유실 방지
- 과거 평문 SharedPreferences 데이터를 암호화 저장소로 migration한 뒤 제거

주의:

- 데이터가 외부 서버로 전송되지 않는 local-first 제품인 것은 확인됨
- key 저장에는 fallback 경로가 존재하므로 `OS 보안 저장소에만 키를 보관`한다고 단정하지 않음
- clipboard 특성상 민감 정보가 저장될 수 있으므로 암호화와 local-only 구조를 핵심 안전 설계로 설명

### 검증과 제품화

- 저장 암호화·backup 복구·잘못된 key·대형 payload·이미지 수백 건·저장 재시도 관련 테스트 존재
- 검색, 그룹, multi-select, drag-and-drop, preview, edit와 keyboard interaction을 포함해 총 56개 test/testWidgets 확인
- Windows Store용 MSIX `1.0.8.0` 설정 확인
- Microsoft Store 제품 페이지에서 실제 공개 상태 확인
- 별도 제품 소개 페이지와 개인정보 처리방침 제공
- 사이트 표기 가격은 1,200원이지만 현재 판매 가격은 Store 제출 상태에 따라 달라질 수 있어 포트폴리오 핵심 성과에서는 제외

### 포트폴리오 포지셔닝

단순한 Flutter CRUD 앱이 아니다. `네이티브 OS clipboard와 window lifecycle을 Flutter 제품 경험으로 연결하고, 민감한 clip을 로컬 암호화 저장하면서 실패·종료·대용량 payload까지 다룬 데스크톱 시스템 제품`으로 보여 주는 것이 맞다.

포트폴리오용 초안:

> Copylight는 복사했던 내용을 다시 찾는 것에서 끝나지 않고, 수정·검색·pin 고정과 JSON formatting까지 같은 작업 흐름에서 처리하기 위해 만든 local-first 데스크톱 클립보드 관리자입니다. Flutter UI에 Windows C++와 macOS Swift 네이티브 기능을 연결해 전역 단축키, 이미지 clipboard, 이전 프로그램으로의 직접 붙여넣기와 tray panel을 구현했습니다. 저장 데이터는 AES-GCM 256으로 암호화하고, 대형 payload 분리·backup 복구·저장 재시도·종료 전 flush를 적용해 일상적으로 계속 실행되는 도구의 데이터 안정성을 설계했습니다. Windows 버전은 Microsoft Store에 직접 배포했습니다.

확인 결과:

- clipboard history 필요
- 저장된 clip 수정 기능 필요
- 자주 쓰는 clip pin 고정 필요
- clip 검색 필요
- JSON formatting을 clipboard 안에서 바로 처리할 필요

## Independent Product 04: Bucket Studio

로컬 저장소 `/Users/uwanggood/Documents/workspace/bucket-studio`의 Flutter 코드, S3 통신 구현, 배포 문서와 Microsoft Store 설정을 확인했다.

### 제품 정의

- AWS S3와 S3-compatible object storage를 하나의 데스크톱 file explorer 경험으로 다루는 local-first 제품
- AWS S3, Cloudflare R2, Wasabi, MinIO, NCP Archive와 사용자 지정 S3 endpoint 지원
- Flutter로 Windows·macOS·Linux용 UI를 구성하고 별도 중계 서버 없이 provider endpoint에 직접 연결
- 2026년 6월 22일 최초 커밋, 현재 앱 버전 `0.1.9+19`
- Windows 버전을 Microsoft Store Product ID `9PHPR8970TN2`로 배포하도록 구성
- 사용자가 제품 기획, 설계, 구현과 Store 배포를 전부 담당

### 만들게 된 실제 배경

- provider마다 다른 웹 console을 오가며 bucket과 object를 관리하는 과정이 불편했음
- Windows에는 S3 Browser 같은 전용 프로그램이 있지만 macOS와 Linux까지 동일하게 사용할 만한 선택지가 부족했음
- 기존 S3 Browser도 전송 속도 제한이 있어 대용량 파일 작업에 불편함이 있었음
- bucket이 많을 때 원하는 bucket으로 빠르게 이동하고 object key의 일부만 검색하고 싶었음
- image·video·text object를 매번 내려받지 않고 앱 안에서 바로 preview할 필요가 있었음
- AWS S3뿐 아니라 업무에서 사용하는 NCP Archive Storage까지 같은 UI에서 지원하는 도구가 필요했음
- 이 문제를 Windows·macOS·Linux에서 공통으로 사용할 수 있는 desktop file explorer 형태의 제품으로 직접 구현

### 기본 사용 흐름

1. provider 종류, region·endpoint와 credential을 등록
2. bucket과 prefix를 file explorer처럼 이동하며 object 검색·선택
3. 파일·폴더를 올리거나 object·folder·bucket 전체를 로컬로 내려받음
4. text·image·video object는 앱 안에서 확인하고 필요한 경우 text를 수정
5. object 복사·이동·이름 변경, presigned URL 생성과 삭제를 수행
6. transfer queue에서 진행률·속도·실패 상태를 확인하고 pause·resume·cancel·retry

### S3-compatible storage 구현

- AWS Signature Version 4의 canonical request, signing key와 authorization header를 앱에서 직접 생성
- AWS session token과 path-style endpoint 지원
- bucket 목록·생성·삭제와 object 목록 pagination 구현
- 일반 provider는 ListObjectsV2, NCP Archive는 호환성에 맞춰 ListObjectsV1 사용
- delimiter와 prefix를 이용해 object storage를 folder 구조처럼 표현
- NCP Archive folder marker에는 provider 특성에 맞는 content type 적용
- upload, download, copy, move, rename과 recursive prefix 작업 구현
- 64MB 이상 파일은 8MB part로 multipart upload하고 실패 시 abort
- part upload는 최대 3회 재시도
- object, folder와 bucket 전체 download 지원
- 동일한 로컬 파일이 존재할 때 overwrite 또는 skip 선택
- download 중에는 partial file을 사용해 미완료 결과가 정상 파일처럼 남지 않도록 처리
- 1시간 유효한 presigned URL을 client에서 직접 생성

### 안전한 object 작업

- 선택 object뿐 아니라 prefix 아래의 모든 대상을 먼저 조회해 삭제 예정 수량과 총용량 표시
- 확인 modal에서 `DELETE`를 직접 입력해야 실제 삭제 실행
- S3 DeleteObjects 제한에 맞춰 최대 1,000개 단위로 요청하고 object별 오류도 확인
- bucket 삭제 전 비어 있는지 확인
- text object는 UTF-8·1MB 이하만 editor로 열 수 있도록 제한
- text 저장 시 기존 ETag를 `If-Match`로 전송해 다른 곳에서 먼저 수정된 object를 덮어쓰지 않음
- HTTP 412를 remote change conflict로 변환해 사용자에게 다시 불러오도록 안내
- copy와 rename은 새 object 생성이 성공한 뒤 기존 object를 삭제하는 순서로 처리

### desktop file explorer 경험

- bucket·folder·object 통합 탐색과 breadcrumb·직접 path 입력
- object name 검색과 단일·다중 선택
- directory path suggestion cache 제공
- file·folder 선택 dialog와 drag-and-drop upload
- image와 video는 presigned URL을 사용해 앱 내부 preview
- text object는 앱 내부 editor에서 열고 저장
- object 또는 prefix 단위 copy·move·rename
- single provider 또는 모든 provider를 read-only drive로 mount하는 rclone 연동
- mount용 임시 rclone config는 session 종료 시 제거하고 credential은 process environment로 전달
- command log와 transfer queue 제공
- transfer별 진행률, 속도, pause·resume·cancel·retry 제공

주의:

- S3-compatible API 범위에서 동작하므로 provider별 완전한 관리 console을 대체한다고 표현하지 않음
- directory upload는 파일을 순회하며 처리하며 현재 코드만으로 병렬 업로드 최적화를 주장하지 않음
- drive mount는 rclone이 설치된 환경에서만 사용할 수 있고 안전을 위해 read-only로 구성

### local-first credential과 장애 대응

- credential과 요청은 Bucket Studio 서버를 거치지 않고 사용자가 지정한 storage endpoint로 직접 전송
- provider credential은 우선 `flutter_secure_storage`를 통해 platform secure storage에 저장
- 기존 account JSON에 평문 credential이 있으면 secure storage로 migration한 뒤 account file을 다시 작성
- account 설정은 temp file 작성 후 교체하고 backup file을 유지
- primary account file을 읽지 못하면 backup으로 복구
- account 제거 시 연결된 secure credential도 함께 삭제
- crash diagnostics와 update channel을 배포 설정으로 분리
- Store 설치본은 Microsoft Store update를 사용하고 과거 direct build는 public manifest를 통해 Store로 이동

주의:

- platform secure storage write가 실패하면 현재 구현은 credential ID 없이 account JSON에 credential을 보존하는 fallback이 존재함
- 따라서 포트폴리오에는 `credential을 원칙적으로 OS secure storage에 보관`한다고 쓰고 `항상 OS 보안 저장소에만 저장`한다고 단정하지 않음
- request·storage path scrub 설계가 있으나 운영 diagnostic에 어떤 정보도 절대 포함되지 않는다고 과장하지 않음

### 검증과 제품화

- service, repository, update, mount와 browser interaction을 포함해 19개 test file 확인
- `test`, `testWidgets`와 `group` 선언 총 124개 확인
- 한국어·영어 UI 제공
- Windows Store용 MSIX `0.1.9.0` 설정과 Partner Center Product ID 확인
- 공개 download repository, privacy·security 문서와 Store redirect update manifest 구성
- Microsoft Store URL: `https://apps.microsoft.com/detail/9PHPR8970TN2`
- 소스는 비공개로 유지하고 public surface에는 Store 배포, 제품 설명과 보안 문서만 공개

### 포트폴리오 포지셔닝

단순한 storage CRUD UI가 아니다. `S3 protocol을 직접 구현하고 대용량 transfer·충돌 방지·recursive delete·credential lifecycle을 데스크톱 file explorer 경험으로 묶은 cross-platform system product`로 보여 주는 것이 맞다.

포트폴리오용 초안:

> Bucket Studio는 provider별 웹 console을 오가는 불편과 운영체제마다 달라지는 S3 관리 도구의 한계를 해결하기 위해 만든 cross-platform object storage browser입니다. bucket 바로가기, object 부분 검색과 image·video·text preview를 제공하고 AWS S3, Cloudflare R2, Wasabi, MinIO뿐 아니라 NCP Archive Storage까지 하나의 desktop file explorer 경험으로 연결했습니다. 별도 중계 서버 없이 AWS Signature V4 요청과 presigned URL을 client에서 직접 생성하고, 앱 자체 전송 속도 제한을 두지 않은 multipart upload·folder 및 bucket download·object copy와 rename·text conflict detection을 구현했습니다. 대량 삭제 preview와 local credential·backup 복구까지 포함해 지속적으로 사용하는 운영 도구의 안전성도 함께 설계했습니다.

현재 확인 결과:

- S3-compatible provider를 하나의 UI에서 탐색
- Windows·macOS·Linux에서 동일한 file explorer 경험 제공
- bucket 빠른 이동, object 부분 검색과 앱 내부 preview
- 대용량 multipart upload와 file·folder·bucket download
- presigned URL, preview, text edit와 ETag 충돌 방지
- recursive delete preview와 명시적 확인
- local-first credential 관리와 account backup 복구
- read-only drive mount와 transfer queue
- Microsoft Store 배포 구성

## Code Archive: 2022–2024 이전 프로젝트 실코드 감사

사용자가 제공한 아래 두 프로젝트 묶음을 실제 코드 기준으로 확인하기 시작했다.

- `/Users/uwanggood/Downloads/drive-download-20260831T070938Z-1-001`
- `/Users/uwanggood/Downloads/drive-download-20260831T070938Z-1-002`

포함된 archive:

- `java17_rpa.zip`: Java 17·Spring 기반 RPA 운영 및 스케줄링 backend
- `fastapi.zip`: Python·FastAPI 기반 AIMOS 검수 backend
- `electron.zip`: Electron·Vue·TensorFlow.js 기반 local inference client
- `app.zip`: Node.js·Express·Sequelize 기반 운영 backend
- `infer.zip`: Node.js·TensorFlow.js GPU inference server
- `mntr.zip`: Node.js backend와 Vue monitoring frontend
- `flutter.zip`: Flutter frontend와 FastAPI backend로 구성된 초기 AIMOS system

### 공개 원칙

이 자료는 실제 작성 코드의 근거로는 가치가 높지만 raw archive 또는 원문 file을 공개하면 안 된다.

확인된 위험:

- `.env.local`, `.env.develop`, `.env.production` 포함
- Git metadata와 remote history 포함
- 사내 package name, DB schema·table·column name과 interface code 포함
- 운영 log와 build artifact 포함
- 과거 Flutter source에 camera·message broker credential이 직접 들어 있는 부분 확인
- 고객사·현장 업무 흐름을 추정할 수 있는 domain identifier 다수 포함

갤러리에는 다음 절차를 통과한 발췌본만 사용한다.

1. 실제로 사용자가 작성한 file과 method인지 확인
2. 해결한 기술 문제를 한 문장으로 정의
3. company·customer·site·table·queue·interface identifier 전부 일반화
4. host, IP, URL, credential과 개인·현장 data 제거
5. 전체 source가 아니라 핵심 algorithm이 이해되는 최소 범위만 발췌
6. 원본과 비식별본을 대조해 동작 구조가 임의로 새로 작성되지 않았는지 확인
7. secret scanner와 수동 검토를 통과한 code만 portfolio data에 반영

### 1차 대표 후보

#### Backend · Java — 충돌을 재배치하는 RPA scheduler

원본 후보:

- `java17_rpa.zip`
- `JobSchedulerServiceImpl.java`
- 약 646 lines

코드에서 확인한 내용:

- cron expression을 하루 동안의 예정 작업 시간으로 전개
- 과거 실행 시간을 기반으로 release별 예상 소요 시간 적용
- 실행 중·대기 중 작업과 예정 작업을 하나의 robot별 시간표로 통합
- 특정 robot, 전체 robot과 동적 할당 전략을 각각 처리
- 비어 있는 robot을 먼저 찾고 없으면 가장 빨리 사용 가능한 robot 선택
- 실행 시간 중첩 시 우선순위, 최초 예정 시간과 현재 실행 여부를 비교
- 우선순위가 낮은 작업을 뒤로 이동한 뒤 연쇄 충돌을 재귀적으로 다시 계산
- 같은 작업이 재배치 과정에서 순환 충돌하면 원래 시간으로 복구하고 fault 후보로 표시
- 장애 작업을 가용 robot과 빈 시간에 다시 시작하고 master·detail·handler 상태 저장

갤러리 설명 후보:

> 실행 자원이 겹치지 않도록 빈 robot을 우선 배치하고, 충돌 시 작업 우선순위와 원래 예정 시간을 기준으로 시간표를 연쇄 재계산하는 RPA scheduler입니다.

판정:

- Java 대표 코드로 사용할 가치가 높음
- 업무 entity와 API 이름을 모두 제거한 뒤 scheduler core만 70~110 lines로 재구성
- 기존 웹사이트의 임의 작성 Java scheduler snippet은 이 실제 코드 기반 발췌본으로 교체

#### Archive evidence · Python — model response를 검수 상태로 반영하는 message orchestrator

원본 후보:

- `fastapi.zip`
- `app/mq/handler/mq_handler.py`
- 약 220 lines

코드에서 확인한 내용:

- RabbitMQ message의 interface ID에 따라 handler dispatch
- model 처리 상태와 현장 검수 상태를 연결
- 모든 상태 변경을 process log로 기록
- 이미 종료된 상태는 조건부 update로 보호해 늦게 도착한 message가 최종 상태를 덮어쓰지 않도록 처리
- 검출 결과가 없을 때, 이물질이 검출됐을 때와 정상 class 결과를 각각 분기
- layer·product·detail 데이터를 bulk insert
- 판정 통계를 계산해 original result와 latest result를 upsert
- client topic으로 상태, 판정 결과와 image 정보를 다시 전송
- handler 실패 시 transaction rollback

갤러리 설명 후보:

> 비동기로 도착하는 model response를 검수 상태 machine에 반영하고, 늦게 도착한 message의 역전 갱신을 막으면서 결과·통계·실시간 client event를 하나의 transaction 흐름으로 처리합니다.

판정:

- 당시에도 model response·DB transaction·실시간 client event를 연결해 운영했다는 경력 근거로만 사용
- Python 대표 갤러리에는 사용하지 않음
- 공개 대표 코드는 현재 `aimos-aim` 또는 `aimos-aib`에서 더 깊고 최신인 flow를 선정

#### Archive evidence · Flutter — 실시간 검수 상태 projection

원본 후보:

- `flutter.zip`
- `ais_frontend/lib/viewmodel/inspct_detail_vm.dart`

코드에서 확인한 내용:

- 검수 시작·일시정지·중지와 상세 정보 갱신
- STOMP topic을 검수 건별로 subscribe
- 실시간 message를 시간 역순으로 정렬해 process log에 반영
- 상태 code별로 layer image와 전체 통계를 갱신
- 다른 client가 상태를 먼저 변경했을 때 refresh 불가 상태를 사용자에게 알림
- 종료 상태를 구분해 local model projection 갱신
- 화면 종료 시 STOMP connection과 subscription 정리
- web 환경에서 RTSP player를 연결하는 분기 포함

판정:

- frontend에서 backend event를 화면 state로 투영한 과거 구현 근거로만 사용
- 동일 file 안에 hardcoded credential이 있어 원본 공개 금지
- Flutter 대표 갤러리에는 사용하지 않음
- 공개 대표 코드는 현재 `aimos-stitch`에서 component system·state·desktop integration이 드러나는 code로 선정

#### Desktop · Electron — local AI inference application lifecycle

원본 후보:

- `electron.zip`
- `electron/main.js`
- `electron/preload.js`
- `electron/core/handler/scriptHandler.js`
- `electron/core/infer/infer.js`

코드에서 확인한 내용:

- frameless desktop window와 tray application lifecycle 구성
- camera·microphone 권한 상태를 확인하고 OS permission 요청
- `nodeIntegration: false`, `contextIsolation: true`로 renderer와 main process 분리
- preload `contextBridge`로 허용된 IPC surface만 renderer에 노출
- desktop capture, config, alert·confirm과 local inference를 IPC handler로 연결
- TensorFlow.js graph model을 main process에서 한 번만 load
- base64 camera frame을 tensor로 decode하고 normalization·batch dimension 처리
- score threshold를 넘는 detection만 renderer로 반환
- TensorFlow scope와 tensor dispose를 사용한 native memory 관리
- tray restore·minimize·quit, native notification과 click routing
- differential update 확인, download와 restart·install flow
- 개발·배포 환경에 따라 devtools와 load URL 분리

갤러리 설명 후보:

> Web UI와 native runtime을 분리한 Electron application에서 camera frame을 main process의 TensorFlow.js model로 전달하고, 권한·IPC·tray·notification·update lifecycle까지 하나의 desktop product로 구성했습니다.

판정:

- Electron 대표 코드로 사용할 가치가 높음
- inference function 하나만 보여 주면 단순해 보이므로 `secure window → contextBridge → IPC inference → native lifecycle` 흐름으로 제시
- 제품 전체 구조를 3개 file의 연결된 발췌본으로 보여 주는 gallery 형식이 적합

#### Backend · Node.js — multi-tenant realtime session hub

원본 후보:

- `app.zip`
- `app/core/socket/socket.js`
- `app/routes/model.js`
- `app/middlewares/tenancyMiddleware.js`

코드에서 확인한 내용:

- tenant token을 검증해 request와 socket scope에 tenant ID 반영
- tenant와 room을 조합한 Socket.IO room 구성
- Redis hash에 사용자별 socket ID와 admin connection 저장
- 접속·종료 시 사용자 상태를 room과 관리자 client에 전달
- 사용자 chat과 AI 이상 징후를 tenant·room 범위에서 실시간 중계
- model 학습 완료 callback을 Redis의 model ID·socket ID와 대조
- 대상 사용자의 socket으로 성공·실패 result를 전달하고 Redis state 초기화
- Express route에 access token과 tenancy middleware를 구분 적용
- Helmet security header와 production path 분기 적용

갤러리 설명 후보:

> tenant와 room 단위로 격리된 Socket.IO session을 Redis에 추적하고, 사용자 상태·chat·AI event와 비동기 model callback을 올바른 client에게 전달하는 realtime backend입니다.

판정:

- Node.js 대표 코드로 사용할 가치가 있음
- 단일 CRUD route가 아니라 `token scope → Redis connection registry → Socket.IO room routing → model callback` 흐름으로 보여 줌
- tenant·room·event·model identifier는 모두 일반화

### 대표 코드에서 제외하거나 보조 자료로 둘 항목

#### Node model wrapper 단독

- TensorFlow.js graph model load, image tensor 변환, threshold filtering과 scope cleanup 구현은 확인
- Electron 전체 runtime의 일부로는 가치가 있음
- 그러나 inference function만 떼어 Model 대표 코드로 내세우기에는 단순함
- Model gallery에는 OCR model architecture, Venus experiment pipeline 또는 Stitch SAM workflow처럼 더 깊은 현재 코드를 우선 사용

#### 초기 Node database wrapper와 query

- Sequelize 공통 select·insert·update wrapper와 raw SQL query file 확인
- 일부 method가 database error를 log만 남기고 상위로 전달하지 않는 구조
- Python dynamic SQL utility에는 문자열 condition evaluation과 query text 치환이 포함돼 있어 현재 기준으로 안전한 대표 구현이라 보기 어려움
- 기술 성장 과정을 보여 주는 archive 근거로만 두고 DB·Query 대표 코드는 Esther 또는 현재 AIMOS code에서 선정

#### 현재 기준으로 숨겨야 할 옛 구현 한계

- face verification 외부 service가 실패했을 때 성공으로 처리하는 fail-open fallback 확인
- 일부 비동기 DB insert를 `await`하지 않고 응답하는 route 확인
- 공통 DB wrapper 일부가 오류를 상위로 전달하지 않고 log 후 빈 결과처럼 처리
- frontend에 credential을 직접 포함한 부분 확인
- 이런 부분은 삭제하거나 고쳐 쓴 코드를 사용자의 실제 코드인 것처럼 제시하지 않음
- portfolio에서는 당시 제품 구조와 해결한 문제를 보여 주되 현재의 권장 보안 구현으로 포장하지 않음

#### archive 전체·build artifact·model weight

- executable, model shard, compiled class, package cache와 log는 코드 역량을 설명하지 못하고 공개 위험만 높음
- portfolio asset으로 복사하지 않음

### Code Gallery 구성 결론

- Java: RPA resource·time conflict scheduler
- Python: 현재 `aimos-aim` 또는 `aimos-aib`의 backend orchestration code
- Flutter: 현재 `aimos-stitch`의 component system 또는 complex interaction code
- Desktop: Electron secure IPC·local inference·native lifecycle
- Node.js: Redis·Socket.IO multi-tenant realtime session hub
- Backend 추가 후보: 현재 AIMOS AIB/AIA 통합 구조에서 선정
- DB: Esther schema diff·versioning 또는 현재 운영 migration code
- Query: 실제 성능 개선 전후가 설명 가능한 AIMOS query
- Model: `/Users/uwanggood/IdeaProjects/plate_ocr`의 OCR architecture·training pipeline

즉, 이 두 archive는 버리지 않는다. 다만 모든 언어를 억지로 채우는 source pool이 아니라 `예전에도 실제 운영 문제를 코드로 해결해 온 증거`로 사용하고, 현재 코드보다 강한 부분만 대표 gallery에 올린다.

### 대표 코드 source 확정

| Category | 공개 대표 source | 이전 archive의 역할 |
|---|---|---|
| Java | `java17_rpa.zip` scheduler 또는 더 강한 현재 Java code와 비교 후 결정 | 실제 scheduling algorithm 후보 |
| Python | `/Users/uwanggood/Documents/workspace/workspace-cmpny/aimos-aim`, `/Users/uwanggood/Documents/workspace/workspace-cmpny/aimos-aib` | 과거 architecture 증거만 사용 |
| Flutter | `/Users/uwanggood/Documents/workspace/workspace-cmpny/aimos-stitch` | 초기 실시간 UI 경험 증거만 사용 |
| Electron | `electron.zip` | secure IPC·local inference desktop flow 사용 |
| Node.js | `app.zip` | Redis·Socket.IO realtime backend flow 사용 |
| Model | `/Users/uwanggood/IdeaProjects/plate_ocr` | archive TensorFlow.js wrapper는 보조 자료 |
| DB·Query | Esther 또는 현재 AIMOS code | 초기 wrapper·raw query는 성장 기록만 사용 |

## 이후에 다룰 항목

- Code Archive 후보의 실제 비식별 발췌본 제작과 secret scan
- 이력 질문 기능 또는 로컬 LLM은 콘텐츠 정리 이후 검토
